"""建筑装饰(建筑施工/工程/园林/装饰)财报专家(轻量版·P0 数值层)。

⚠️ 非投资建议;阈值/区间/权重均为**工程占位**,注释注明"待标定"。

行业口径:
- 低毛利、垫资施工:工程款按进度结算、质保金滞留 → 应收/合同资产巨大、回款慢、经营现金流常年偏紧。
- 高杠杆:垫资靠融资,资产负债率天然高,通用『高负债』系统性误杀 → SKIP,健康维放宽。
- 命门:应收随营收膨胀(垫资越大回款越慢)+ 经营现金流持续为负=资金链风险(施工企业暴雷典型)。

只写本文件;不改共享文件。纯函数、无网络、无状态;缺值→None/不命中,不抛异常。
"""
from __future__ import annotations

KEY = "建筑装饰"
NOTE = ("建筑施工/工程口径:低毛利垫资施工(毛利率区间下压)、高杠杆(『高负债』已 SKIP、资产负债率反向区间放宽)、"
        "应收/合同资产巨大回款慢(应收周转天数大幅放宽);命门=应收膨胀+经营现金流持续为负=资金链风险。阈值工程占位待标定。")


def dimension_specs() -> dict:
    """拷通用默认再改目标区间:毛利率下压(低毛利)、资产负债率放宽(高杠杆)、应收周转大幅放宽(垫资)。工程占位待标定。"""
    return {
        "成长": [("营收增速%", "营收增速", -20, 40),
               ("扣非净利增速%", "扣非净利增速", -20, 40)],
        # 质量:施工低毛利,毛利率 100分端下压到 20【待标定】
        "质量": [("现金含量 CFO/归母净利", "现金含量_CFO比净利", -0.5, 1.0),
               ("扣非占归母", "扣非占归母", 0, 1.0),
               ("毛利率%", "毛利率", 0, 20)],
        # 健康:垫资高杠杆,资产负债率反向区间放宽(通用 80→30 近乎全灭)【待标定】
        "健康": [("资产负债率%(反向)", "资产负债率", 85, 45),
               ("短债覆盖", "短债覆盖", 0, 1.5),
               ("商誉占净资产%(反向)", "商誉占净资产", 50, 0)],
        # 运营:垫资施工回款极慢,应收周转天数大幅放宽【待标定】
        "运营": [("应收周转天数(反向)", "应收周转天数", 360, 90),
               ("存货周转天数(反向)", "存货周转天数", 360, 60)],
        "回报": [("ROE%", "ROE", 0, 15)],
    }


def weights() -> dict | None:
    return None


# 高杠杆属施工行业特性(垫资靠融资),通用『高负债』系统性误杀 → SKIP。
SKIP_FLAGS = ["高负债"]


# ── 专属红旗阈值(工程占位,待标定)────────────────────────────
_AR_GAP_PCT = 20.0     # 应收增速−营收增速 超此(pct)→ 垫资越滚越大、回款恶化


def _num(rec, *path):
    cur = rec
    for k in path:
        if not isinstance(cur, dict):
            return None
        cur = cur.get(k)
    return cur if isinstance(cur, (int, float)) else None


def extra_flags(derived: dict, structured: dict) -> list[dict]:
    """建筑装饰专属红旗。仅 命中=True 者并入;缺值不命中不抛异常。阈值工程占位待标定。"""
    derived = derived or {}
    out: list[dict] = []

    def emit(code, sev, val):
        out.append({"code": code, "命中": True, "严重度": sev, "值": val})

    # 1) 应收膨胀于营收(垫资回款恶化):应收增速远超营收增速。
    _ar = derived.get("应收营收增速差")
    if _ar is None:
        a, r = derived.get("应收增速"), derived.get("营收增速")
        _ar = (a - r) if (a is not None and r is not None) else None
    if _ar is not None and _ar > _AR_GAP_PCT:
        emit("应收膨胀", "中", {"应收营收增速差pct": round(_ar, 4), "阈值pct": _AR_GAP_PCT,
                            "NOTE": "垫资施工应收异常膨胀,回款恶化"})

    # 2) 经营现金流为负(账面盈利却现金失血):施工企业资金链核心风险。
    归母净利 = _num(structured, "利润表", "归母净利润")
    CFO = _num(structured, "现金流量表", "经营活动现金流量净额")
    if 归母净利 is not None and 归母净利 > 0 and CFO is not None and CFO < 0:
        emit("经营现金流为负", "中", {"归母净利润": 归母净利, "经营活动现金流量净额": CFO,
                              "NOTE": "账面盈利却经营现金净流出,叠加高杠杆需警惕资金链"})

    return out
