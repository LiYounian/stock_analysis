# 财报分析 financial_report · 开发包(需求与接口)

> 层:分析层(个股评估的基本面深挖维度)· 状态:**🔒 阻塞 N2**(待用户定框架)· 分支建议:`feat/financial-report`
> 参考:[需求与目标.md](../../docs/需求与目标.md) N2

## 职责(做什么 / 边界)
- 对选定个股做**财报深度分析**:盈利质量 / 成长性 / 现金流质量 / 偿债 / 杜邦分解 / 风险点。
- 可复用 `collectors/fundamental`(已有摘要)+ 未来三大报表原文;定性/原文抽取部分走 `tools/llm`。
- **边界**:纯分析,输出结构化 JSON;不采集(经 collectors)、不落中心记录(经 serialize)。

## 接口契约(待 N2 定稿)
```python
def analyze(code: str) -> dict:
    """输出方向:{盈利质量, 成长性, 现金流质量, 偿债能力, 杜邦分解, 风险点, 结论}。"""
```

## 阻塞:待用户讨论(N2)
- 看哪些财务维度?产出什么结论?哪些字段用 LLM 从财报原文抽取?
- 解冻后:先写 `docs/计划/财报分析.md` 定框架 → 再开发。

## 验收标准(占位)
- [ ] 关键财务维度可计算;定性部分 LLM 抽取带缓存;输出过契约。
