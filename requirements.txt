# 核心数据源(基座)
akshare>=1.18
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # parquet 缓存

# 采集辅助
requests>=2.31
lxml>=4.9              # akshare 部分接口解析依赖
curl_cffi>=0.16        # 伪装浏览器 TLS 指纹,解锁东财(资金流等),绕过 JA3 反爬

# Web 展示(FastAPI + Jinja2,uvicorn 起服务)
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
httpx>=0.27              # FastAPI TestClient(测试用)

# LLM(情绪/新闻抽取,OpenAI 兼容,走用户环境变量配置的网关)
openai>=1.40

# 行业成分(个股→证监会行业,供形态选股 RS 双层的板块归属)。自有协议,不走东财 push2,
# 绕过本机对东财 kline/clist 路径的网络墙。
baostock>=0.8.9

# 存储 DB 后端(SQLite 起步,可换 Postgres/MySQL)+ 定时调度
SQLAlchemy>=2.0
APScheduler>=3.10
# pymysql>=1.1         # 仅当 DB_URL 用 mysql+pymysql:// 时需装
# psycopg[binary]>=3.1 # 仅当 DB_URL 用 postgresql+psycopg:// 时需装

# 测试
pytest>=8.0

# 缺口备选(可选,按需装)
tushare>=1.4.0          # 全 A 每日批量日线(TUSHARE_TOKEN)，盘后增量主源
# scikit-learn / scipy  # P3-E ML 预测阶段再装

# 精确版本见 requirements-lock.txt(conda 环境 stock_analysis 冻结)
