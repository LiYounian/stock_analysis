# 采集缓存(数据不进版本库)
data/raw/

# 本地 DB 文件(SQLite 存储后端,不入库;线上库走 DB_URL 指向的独立实例)
data/*.db

# 上传回执(本地端运行态,不入库)
data/sync_receipts/

# 回测本地产物(HS300 缓存 / 前瞻回测 JSON / 运行日志;可由复现命令重生成,不入库)
data/backtest_local/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# 系统
.DS_Store

# 注:data/analysis 与 data/reports 暂**保留跟踪**——合作者靠 git 拉取数据看网页,
# 在"展示端 + DB"上线(见 计划/展示端与数据同步.md)前,数据仍走 git 传输。届时再移出。
docs/报告/

# 内部沟通/需求澄清文档(本地保留,不入库;已从仓库根迁至 docs/ 下,仍不入库)
docs/需求沟通/

# 密钥/本地配置
.env
*.token

# Claude Code 本地配置,不入库
.claude/

# 滚动主档(全A历史,几百MB,本地/远端各自维护,不入库)
data/master/
scratch_diag_out.txt
data/analysis/backtest/finval/

# 派生聚合总表(每日由 tools/analysis/panel.py:write_panel 重写,可从 data/analysis/<日期>/*.json
# 再生)。曾被跟踪 → 每轮产生 diff、把主仓工作树弄脏、挡 merge --ff-only,故取消跟踪并忽略。
# 精确到这两个派生文件,不误伤 data/analysis/<日期>/*.json 等仍需跟踪的原始数据。
data/analysis/panel.csv
data/analysis/panel.json

# 次日实盘口径累积胜率牌（复盘任务每日 append 的派生 KPI 滚存，可从复盘 md 再生）。
# 随 data/analysis/backtest/*forward_scorecard.csv 惯例:派生数据不入库,避免每日 diff 弄脏工作树。
data/analysis/backtest/nextday_scorecard.csv

# 统一大复盘汇总记分卡（tools/review 生成的派生 KPI 滚存，可从产物+kline 再生，不入库）。
data/analysis/backtest/grand_review_scorecard.csv

# 定时任务运行期产物（护栏锁 / OS级盘中快照），纯滚存不入库
data/task_locks/
data/intraday/

# 每日 forward 影子跑证据库（v2r 扩样，纯 dry-run 旁路滚存，不入库；同 forward_scorecard 机制）
data/shadow_forward/

# 消息驱动板块研判每日增量产出(catalyst_<date>.json 含真实新闻原文 + LLM 分级研判、
# analyzed/ 去重滚动归纳):每日重生的派生数据、含新闻内容,不入库(脱敏·避免每日 diff 弄脏)。
# 面向合作者的摘要「消息驱动」块已并入 tracked 的 data/analysis/<date>/sector_focus.json。
data/sector_news/

# 运行日志不入库
logs/

# capitulation 信号节点(com.stock.capitulation_signal)每日生成的派生数据:
# trailing 广度缓存(增量重建) + 逐日 β 择时信号。生产数据在主仓、按需重建,不入库。
data/breadth_series/
data/signals/

# 程序化流水线 S1/S2 周度派生过程文件(量价重算·可复现·自带 roster_changes/history 变更留痕):不入库,避免每周 diff 弄脏。
data/analysis/sector_universe.json
data/sector_roster_table/

# 盯盘监控运行时产物(watchlist + alert流水),不入库
data/watch/
