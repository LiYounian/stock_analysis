# H1~H5 数据地基缺陷 · 根因诊断与修复方案

> 日期:2026-09-18 · 状态:**诊断 + 方案,未执行**(执行须统筹确认后另派) · 范围:A4 数据字典(`docs/计划/2026-09-17_A4_数据维度IO数据字典.md`)报出的五条横切事实 H1~H5
> ⚠️ 研究模拟、非投资建议。本文不改任何生产代码 / 定时任务配置 / 主仓数据。
> 方法:读 `~/.local/state/stock/*.log` → 划范围 → 读代码定根因(文件:行)→ 只读 python 复核数据 → 出方案 + 预期效果 + 核法。H2~H5 由 4 个并行只读子调研完成,统筹窗口单独核实过 H1/H2 成立。

## 0. 一页结论

| # | 一句话根因 | 今晚(09-18 15:40)前必修? | 工作量(token / agent 工时) |
|---|---|---|---|
| H1 | launchd 进程 fd 软上限 256 × 09-13 把两层 LLM 并发都从 4 调到 10(峰值在飞 100~200 连接)× `get_client()` 每次新建从不关闭的 OpenAI/httpx 客户端 → EMFILE;收盘 `run_screen_all` 里 `enrich_candidates` 及其后 7 步裸调无 `_safe`,午盘同 bug 却被 `_safe` 静默吞掉 | **是**(F1 止血 + F2 ulimit + F3 并发回 4;否则今晚第 5 天崩,金字塔 v1 又喂午盘数) | 止血 ~25K / 1h;根治(F4/F5+测试)~120K / 3h |
| H2 | 腾讯两个端点(gtimg 快照 + fqkline 回退)对 688/689 返回的 volume 已是"股",`market.py:446/:114` 与 `intraday_screen.py:271` 一律 ×100 → **618 只(617 只 688 + 689009)自 08-13 起**(非 09-08)约 13,921 行 volume 100×,08-13~09-03 turnover 也被连带 100×;全帧中位数断言对 11% 少数派失明 | **是**(先合代码修复否则今晚再写一天坏行;再 baostock 全量重算 618 只,避开 15:40~17:00) | 修 60~90K/1~1.5h + 回补 30~40K/0.5~1h → **~120~155K / 2~3h** |
| H3 | 数据根 = 代码 worktree(`settings.PROJECT_ROOT`),无 env 开关;dailyjob 已 symlink `master/raw/intraday/backtest_local` 回主仓,但 `analysis/breadth/sync_receipts/shadow_forward` 未做,`mirror_to_main` 白名单只 3 类 | 否(周内;breadth 4 日先 `cp -p` 回补即可) | C2 symlink ~30K / 1h;Phase-2 根治 ~400K / 12h |
| H4 | 三条 α 基准链数学不等价、无一落盘:链② `build_market_index.py` 忽略 `--data-root` 且从未被调度;链① 读 breadth 受 H3 断档 | 否(周内;但 D+2 回填/大复盘 α 需要它,建议 H3 回补后立刻做) | ~60K / 2h(含 08-01 起回算) |
| H5 | 记分卡各自为政:单位百分数 vs 小数、入场 close_T vs open_T1 vs 限价,无 `unit/entry_rule` 字段 | 否(周内) | ~80K / 3h |

**今晚前必修清单(按序)**:H1-F2(ulimit)→ H1-F3(并发 env 回 4)→ H1-F1(`_safe` 包裹)→ H2 代码修复 → H2 主档 baostock 重算 618 只 → 09-17 收盘回补(可选,仅 15:40 前有效)。合计 **~115~165K token / 2.5~3.5h** agent 工时(含用户审阅);H1 与 H2 两组不改同一文件,可两窗口并行。另建议今晚顺手 20 min:H3 breadth 四日 `cp -p` 双向回补 + H5 的 A4 字典 F3 勘误。

---

## H1 · 收盘闭环 `screenall` 连续 4 日 EMFILE 崩溃

### 1.1 现象(日志实证,`~/.local/state/stock/pull_refresh.log`)

| 交易日 | screenall 结局 | 崩点 | 崩前已跑到 | 当日 EMFILE 条数 |
|---|---|---|---|---|
| 09-11 | ✅ 完成(union 296 / llm_subset 308 / analysis_set 378) | — | — | 0 |
| 09-14 | ❌ `!! screenall 失败` | `run.py:1267 run_screen_all → enrich_candidates → run_sentiment` | 步骤② 9 策略视图已落盘;数值面补缺已做;新闻采完;**LLM 政策打分后、逐票情绪并发中** | ~170 |
| 09-15 | ❌ 同上 | `run.py:1271` 同链 | 同上 | ~170 |
| 09-16 | ❌ 同上 | 同上 | 同上 | ~170 |
| 09-17 | ❌ 同上 | 同上 | 同上(382 候选,新闻需采 50) | ~190 |

全日志 711 条 `[Errno 24]`,702 条集中在 09-14~17 四天;此前只有 09-03 出现过 9 条(当时富集并发=4),均被 `news_persistence.classify` / `event._resolve_layer` 自身的 try/except 吞掉、未上抛。

最终致命 traceback(09-17,行 1091150~1091228):

```
run.py:1271 run_screen_all → enrich_candidates
run.py:591  run_sentiment(codes, workers=_enrich_workers())      # workers=10
run.py:344  parallel.pmap(_one, codes, workers) → fut.result()   # 子线程异常原样上抛
run.py:332  event.analyze_stock(code)
event.py:588 _resolve_layer("ugc") → legacy_loader → ugc.load_ugc
repo.py:223 get_raw → :135 _resolve_read_date → :118 _date_dirs_desc(root)
pathlib.iterdir → os.listdir('.../dailyjob/data/raw')
OSError: [Errno 24] Too many open files
```

`os.listdir(data/raw)` 只是**受害者**(它需要 1 个 fd 而已),不是泄漏点。

午盘路径(`intraday_screen.log`)同样撞 EMFILE 但**没崩**:09-14 161 条 / 09-15 74 / 09-16 68 / 09-17 35,每天都触发一次 `run WARNING 候选池三层情绪+news_ai 采集失败,降级跳过` —— 因为 `candidate_message.py:258` 用 `run._safe(...)` 包了 `run_sentiment`,而收盘 `run_screen_all` 里 `enrich_candidates`(:1271)以及其后 `collect_ticks / run_serialize / run_events / run_factor / run_council / run_panel / run_screen`(:1274~1283)**全部裸调**。同一个 bug,午盘静默降级,收盘整条闭环中止。

### 1.2 根因(三因叠加,缺一不崩)

| # | 因子 | 证据 | 性质 |
|---|---|---|---|
| R1 | **launchd 进程 fd 软上限 = 256** | `launchctl limit maxfiles` → `256 unlimited`;`ops/launchd/*.sh`、`_bootstrap.sh`、`tools/` 全仓 **零处** `ulimit -n` / `resource.setrlimit`;交互 shell 是 1048576,故手跑从不复现 | 环境 |
| R2 | **嵌套并发 10×10 + 09-13 B1 提速把两层都调高** | 外层 `run_sentiment` `ENRICH_WORKERS` 4→10(`config/strategy.py:560`);内层每票新闻条目 `event.extract_news_events` `_pmap(..., settings.LLM_EXTRACT_WORKERS)` 4→10(`settings.py:75`);`attach_persistence → classify_batch` 又一层 `LLM_EXTRACT_WORKERS`。峰值在飞 HTTPS 连接 16 → **100~200**。commit `999d99c`/`cb77a36` 2026-09-13 19:52 合入,**下一次收盘(09-14)即首崩** | 触发 |
| R3 | **LLM client 按调用新建、从不关闭** | `event.py:224/370/489`、`news_persistence.py:84/96`、`news_ai.py:86` 都是 `client = client or lc.get_client()`;`client.py:185 get_client` 每次 `OpenAI(...)` 新建(无缓存/无 `close`,grep 零命中);openai 2.53 默认 `Limits(max_connections=1000, max_keepalive_connections=100, keepalive_expiry=5s)`,keep-alive 连接挂在未回收的 client 上等 GC | 泄漏 |

再叠加:EMFILE 一旦出现,openai SDK 自动重试(日志见 `Retrying request to /chat/completions`)→ 更多连接 → 雪崩;并发 news 采集(`FETCH_WORKERS` 8→16)在 09-15 午盘已先把 fd 吃到写 `news/300285.json.tmp` 都失败。

**待核 1**(核法):在 `pull_refresh.sh` 开头临时加一行 `echo "nofile soft=$(ulimit -Sn)"`,下次 launchd 触发看日志——预期打出 256。不加代码的旁证:`launchctl print gui/$UID/com.stock.pullrefresh` 不显示 rlimit,只能靶试。

### 1.3 受害产物清单(09-14~17)

以 09-11(最后一个完整日,dailyjob 与主仓逐字节相同)为基线:

| 产物 | 09-11 | 09-14 | 09-15 | 09-16 | 09-17 | 状态 |
|---|---|---|---|---|---|---|
| 9 个策略视图 json | ✅ | ✅ | ✅ | ✅ | ✅ | **收盘版**(步骤② 在崩点前) |
| 逐票 record `<code>.json` | 386 只 22:xx 写 | 84 只 **12:03~12:28** 写 | **7 只**(主仓 14)11:56~12:01 | 84 只 11:52~12:35 | 83 只 **12:44~13:00** 写(主仓 16:05 镜像) | **全部午盘版**:由 11:32 `intraday_screen` 阶段2 `candidate_message._default_serialize` 在午盘注入下 serialize;`snapshot.close`≠收盘 K 线:09-16 **66/84**、09-17 **75/84**(09-11 基线 9/386,为后续前复权改写噪声) |
| `候选池消息面确认.json` / `消息面评分.json` / `消息面确认/` / `sentiment/` | 收盘版 | 午盘版 | **缺**(午盘阶段2 也没跑完) | 午盘版 | 午盘版 | 无 slot 标记,Agent 收盘选股当收盘数据用 |
| `panel.json` / `screen.json` / `估值位监控.json` / `多策略命中闸门.json` / `backtest.json` | ✅ | ✗ | ✗ | ✗ | ✗ | 停在 09-11 |
| `factor/` `chart/` `financial_text/` `news_ai/` | ✅ | ✗ | ✗ | ✗ | ✗ | 停在 09-11 |
| raw `tick` / `tick_summary` | ✅ 374 | ✗ | ✗ | ✗ | ✗ | `collect_ticks` 在崩点后 |
| raw `announcement` / `annual_report_text` | ✅ | ✅(午盘采) | ✗ | ✗ | ✗ | 采集节点在崩点后 |
| raw `lhb` | 109 | 有 | 有 | 86 | 49 | 15:47~15:55 写——**来自其它任务**(veto_track_shadow 等),非 screenall `collect_lhb`;待核 |
| `lhb_forward_scorecard.csv` | 09-11 | ✗ | ✗ | ✗ | ✗ | `_update_lhb_scorecard` 在崩点后 |
| `forward_scorecard.csv` 日增 | 387 | **85** | **7** | **85** | **84** | 行数=午盘 record 数;`trend/senti/council` 列全为午盘口径 |
| `每日选股.json` / Agent 4 份 json / 金字塔 md | — | 有 | 有 | 有 | 有 | 塔尖基于以上午盘版数据产出 |

### 1.4 最小修复(建议顺序 = 今晚必修 → 本周)

**F1(今晚必修,~10 行)止血:让崩溃变降级**
- `tools/run.py:1271` `enrich_report = _safe("候选定向富集", lambda: enrich_candidates(...)) or {}`;
- `:1274~1283` 七个裸调各包 `_safe(label, ...)`(`run_serialize` 是后续一切的前提,失败要 `logger.error` 并写 `_GAP_ALARM` marker,但不中止 panel/screen 之外的独立节点);
- 预期效果:任何单节点异常 → WARNING + 继续,多策略闸门/前瞻回测/龙虎榜/记分卡照跑。

**F2(今晚必修,1 行 + 1 行)抬 fd 上限**
- `ops/launchd/pull_refresh.sh` 与 `intraday_screen.sh`(以及 `lib/` 公共头)在 `cd "$REPO"` 后加 `ulimit -Sn 65536 2>/dev/null || ulimit -Sn 10240`(hard=unlimited,允许);
- 兜底:`tools/run.py main()` 开头 `resource.setrlimit(RLIMIT_NOFILE, (min(65536, hard), hard))`,try/except 不阻断;
- 预期效果:同样并发下 fd 余量 ≥ 100×。

**F3(今晚必修,改 env 不改码)收敛嵌套并发**
- plist / shell 里 `export ENRICH_WORKERS=4 LLM_EXTRACT_WORKERS=4 CANDMSG_WORKERS=6`(回到 09-13 前的实测稳定值);峰值在飞 16~24;
- 09-13 B1 的提速收益(收盘主体 I/O)保留一部分,待 F4 落地后再逐步调回。

**F4(本周)根治 client 生命周期**
- `tools/llm/client.py get_client(purpose)` 加进程级缓存(按 `(provider_id, timeout, thinking)` 键 `functools.lru_cache` 或模块 dict + 锁),`OpenAICompatClient` 加 `close()`,并把 openai 客户端 `http_client=httpx.Client(limits=Limits(max_connections=ENRICH×EXTRACT+8, max_keepalive_connections=8))` 显式收口;
- `event.analyze_stock` / `news_ai.write_news_ai` 入口取一次 client 传到底(已有形参,只是调用方没传);
- 加**内层并发全局闸**:`event._pmap` 的 workers 改为 `min(LLM_EXTRACT_WORKERS, max(1, GLOBAL_LLM_INFLIGHT // ENRICH_WORKERS))`,或一个进程级 `threading.Semaphore(GLOBAL_LLM_INFLIGHT=24)` 包住 `_cached_extract` 的 chat 调用;
- 加 `tests/`:`test_llm_client_reuse`(同 purpose 两次 get_client 同对象)、`test_run_screen_all_enrich_failure_degrades`(monkeypatch enrich_candidates 抛 OSError,断言后续 run_multi_gate 仍被调用)、`test_pmap_nested_inflight_bounded`。

**F5(本周)可观测**
- `run_sentiment` 开头/结尾各 log 一次 `len(os.listdir('/dev/fd'))`(macOS 可用)+ `resource.getrlimit`,并在 `_safe` 捕获到 `OSError errno==24` 时打 ERROR 级 + 写 `data/analysis/<D>/_GAP_ALARM.json`;
- `pull_refresh.sh` 末尾 `test -f data/analysis/$D/多策略命中闸门.json || echo "!!! 收盘闭环残缺"`(与现有 SEPA 护栏同款)。

### 1.5 09-14~17 回补:可行性与步骤

**结论:09-17 可在今天(09-18)15:40 前忠实回补;09-14~16 不能忠实回补,只能"打标 + 记分卡剔除",不建议重跑。**

原因(代码事实):
- `run.py:86 _as_of()` 恒取今天;`screenall` 不接 `--date`(只有 `ticks/enrich/candmsg/shadow_forward` 接);
- `market.load_kline`(:540)→ `repo.get_master_kline`(:369)返回**全历史不按 as_of 截断**;`serialize.build_record`(:288)用 `load_kline_recent` 尾 bar 当 snapshot;`_last_bar_date` 以最后一根 bar 为口径日 → 若主档已含 09-18 bar,重跑 09-14~17 的 record 全是 09-18 收盘口径,**比午盘版更错**;
- `put_raw` 缺省 `active_date()`:重跑时 `collect_values_missing/collect_ticks/collect_lhb` 触网拿到的是 09-18 数据却落进 `raw/2026-09-1x/` 分区(串日)。

**09-17 回补步骤(需统筹批准,建议 09-18 11:00 前完成,避开 11:32 午盘任务并发写)**
1. 只在 dailyjob worktree、`ulimit -Sn 65536`、`export ENRICH_WORKERS=4 LLM_EXTRACT_WORKERS=4 STOCK_AS_OF=2026-09-17`(需 F0 一行 patch:`_as_of()` 读 `STOCK_AS_OF` env 优先);
2. 先核 `master/kline` 5 只样本 `last_date==2026-09-17`(未被 09-18 任何任务 append);
3. `python -m tools.run screenall --no-fetch`(策略视图会被同口径重写,幂等;新闻/LLM 命中 hash 缓存,增量只烧 09-17 崩点之后未打分的票);
4. `python -m tools.backtest.forward_scorecard --out .../forward_scorecard.csv`(record mtime 更新 → 09-17 行自动重算);`python -m tools.run shadow_forward --date 2026-09-17`;
5. `mirror_to_main`(或手动 rsync analysis/2026-09-17 + 两张 csv);
6. 验证:`snapshot.close == kline.close` 全部 84+ 只 100%;`多策略命中闸门.json / panel.json / screen.json / backtest.json` 09-17 存在;`lhb_forward_scorecard.csv` 出现 09-17 行;`forward_scorecard` 09-17 行数从 84 涨到 ~380。
- 预估:LLM 增量 ~300 票 × 三层 ≈ 0.6~1.0M token(网关侧,非本对话);墙钟 40~70 min;agent 值守 1.5 h。
- 若 15:40 前来不及:**放弃 09-17 回补**,与 09-14~16 同处理。

**09-14~16 处理(不重跑)**
- 给这三天 `analysis/<D>/<code>.json` 的 `meta` 补 `session="noon"`、`kline_slot="11:30"`(一次性脚本,只加字段不改值),`候选池消息面确认/消息面评分` 顶层加 `slot="noon"`;
- `forward_scorecard.csv` 这三天的行:标 `signal_session=noon`(H5 字段化后)或在 strategy_scorecard / shadow 回退基准里剔除 09-14~16;
- 真正的收盘口径事后复算(panel/闸门/记分)等 H2/H3 后另立"历史重建按 as_of 截断"专项(`load_kline` 加 `≤active_date` 截断开关是前提,风险中等,非今晚)。

### 1.6 预期效果与验证

| 检查项 | 现状 | 修后预期 | 核法 |
|---|---|---|---|
| 收盘 `screenall` 退出码 / 日志 | `!! screenall 失败` 4 天 | `全A多策略选股完成` 且无 `Errno 24` | `grep -c "Errno 24" pull_refresh.log`(当日段)=0 |
| 逐票 `snapshot.close` vs 主档收盘 | 09-17 9/84 一致 | 当日 100%(隔日复核 ≥97%,差额为前复权改写) | 本文 §1.1 用的 python 脚本(只读) |
| `多策略命中闸门/panel/screen/backtest.json` | 停 09-11 | 当日生成 | `ls data/analysis/$D/` |
| `forward_scorecard` 日增 | 84 | 300~600(回到 09-11 前水平) | `groupby(date).size()` |
| `lhb_forward_scorecard` | 停 09-11 | 当日有行 | tail |
| 午盘 `intraday_screen.log` `Errno 24` | 35~161/日 | 0 | grep |
| fd 峰值 | 未知(>256) | `run_sentiment` 日志打印 ≤ 200 且 rlimit ≥ 10240 | F5 日志 |

### 1.7 影响范围
- 改动文件:`tools/run.py`(_safe 包裹 + `_as_of` env + rlimit 兜底)、`ops/launchd/pull_refresh.sh` / `intraday_screen.sh` / `lib/`(ulimit + env)、`tools/llm/client.py`(F4)、`tools/analysis/event.py`(F4 闸)、`tests/` 新增 3 个;
- **不改**策略逻辑、不改 record 契约、不改 launchd 触发时间;F3 只动 env 默认值(config 已支持覆盖)。
- 风险:F1 让"崩"变"静默残缺",必须同时上 F5 护栏,否则下次坏得更隐蔽;F2 在 launchd 下 `ulimit -Sn` 若被拒(理论上 hard=unlimited 不会)需看 `pullrefresh.err.log`。

---

## H2 · 科创板 688 volume 100× 高估

### 2.1 先纠正前提:坏的不是"自 09-08",是 **自 08-13**,且 turnover 被连带污染

dailyjob 的 `data/master` 是主仓 symlink(H3),**主档只有一份**,修一次即可。腾讯两个端点(批量快照 `qt.gtimg.cn`、K 线 `web.ifzq.gtimg.cn/fqkline`)对 688/689 **都返回"股"**、其余板块返回"手";项目在两处都一律 ×100。

| 时段 | 写入路径 | 688 volume | 688 turnover | amount |
|---|---|---|---|---|
| ≤08-12 | baostock 主档 | 正确 | 正确 | 在 |
| **08-13** | `_fallback → _fetch_tencent`(fqkline)×100 | **100×** | **100×**(`backfill_turnover` 按 100× volume 回填) | NaN |
| 08-14、08-17 | 非腾讯源(值与 baostock 逐位一致) | 正确 | 正确 | 在 |
| **08-18~09-03** | fqkline 回退 ×100 | **100×** | **100×** | NaN |
| **09-04**、**09-09** | fqkline 回退 ×100 | **100×** | NaN | NaN |
| 09-07 | 非腾讯源 | 正确 | 正确 | 在 |
| **09-08、09-10~09-17** | `fetch_spot_all_tencent`(gtimg 快照)×100 | **100×** | 正确 | 在 → 这批被 meta 误标 `turnover_unit_anomaly` |

### 2.2 根因代码行

| # | 文件:行 | 内容 |
|---|---|---|
| R1 | `tools/collectors/gtimg_quote.py:71` | `"volume": _num(parts[6]),  # 成交量(手)` —— 字段不带单位,"(手)"是对非 688 的经验断言(字段 36 与 6 同值) |
| R2 | `tools/collectors/market.py:446` | `fetch_spot_all_tencent`:`vol_shou * 100.0  # 手 → 股` → 09-08 起每日坏行 |
| R3 | `tools/collectors/market.py:114` | `_fetch_tencent`(fqkline):`float(x[5]) * 100` → 回退首选源(`:35 DEFAULT_SOURCES=("tencent",…)`)→ 08-13~09-04、09-09 坏行 |
| R4 | `tools/pipeline/intraday_screen.py:271` | 午盘注入 `vol_hand * 100`(内存不落盘,但当日午盘量能信号同错) |
| R5 | `tools/collectors/master_sync.py:437-455, 512` | 路由到 R2/R3,本身无 ×100 |
| 正确源 | `tools/collectors/baostock_src.py:122-160` | 无缩放,全板块统一"股" |

**为何没被拦住**:`market.py:387-417 _assert_spot_amount_volume` 用**全帧中位数** `amount/(close×volume)`,688+689 只占 618/5566≈11% → 中位数仍≈1 → 通过;`tools/config/units.py:131-148 _rel_series`(turnover/volume 邻域比)在 09-08 后把 volume 100× 记成 **turnover** 异常(`repo.py:415-433` 只 error+写 meta 不改数),08-13~09-03 turnover 与 volume 同为 100× → rel≈1 → **盲区**。

### 2.3 实测(只读 python,5566 只全量 + 在线核对)

| 指标 | 结果 |
|---|---|
| 在线核对(09-17)gtimg f6 `volume×price/(amount×1e4)` | 688981/688802/688795/**689009** = 0.996/1.113/1.010/1.006(**股**);600519/300750/000001/920002 = 0.0100(手);fqkline 688802 09-10 = baostock 2,158,871(股),600519 = 18,900 vs baostock 1,890,022(手) |
| volume 坏(amount 在位且比值∈[30,300])代码数 | **618 = 617 只 688 + 689009**;非 688/689 **0** |
| 坏行按日(amount 在位) | 09-08:614、09-10:614、09-11:614、09-14:615、09-15:616、09-16:617、09-17:617 |
| 回退行(amount NaN)volume 100× | 08-13:599;08-18~09-04 每日 596~608;09-09:598 |
| 坏行总数(含回退行) | **≈13,921 行**(526 只各 23 行) |
| 688 turnover 100× | 08-13、08-18~09-03(如 688795 08-13 566.59% vs baostock 5.666%);09-04/09-09 NaN;09-08 起正确 |
| A4 提的非 688 残留 4 只(08-14/17) | `301707/301717/603468/603190` volume **正常**,是 turnover 侧异常,与本问题无关 |
| meta `turnover_unit_anomaly` | 688 段 611 + 689 段 1 + 301 段 2 + 603 段 2;日期 09-08/10/11/14/15/16/17 各 609~611;688 坏但未标 6 只(新股参考点不足) |

旁证待核:① 首次读 `688981.parquet` 报 "Parquet magic bytes not found",1 分钟后正常 → 疑并发原子写窗口(`repo.py:196-208`),核法:收盘窗口外重读全量统计 `ArrowInvalid`;② 09-07/08-14/08-17 为何正确(疑回退命中 sina 或 baostock),核法 `grep "命中源\|回退" ~/.local/state/stock/*.err.log | grep 2026-09-07`;③ `ops/backfill_turnover.py --apply` 实际执行日期无留痕。

### 2.4 修复 patch 草案(单位归一下沉到 gtimg 采集层,按代码段 + 量额自证)

```python
# tools/config/exchange.py(新增)
def is_star_market(code) -> bool:
    """688xxx + 689xxx(CDR)。腾讯 gtimg 两端点对这两段 volume 为"股",其余为"手"。判据只写这一份。"""
    c = str(code).strip()
    return len(c) == 6 and c.isdigit() and c[:3] in ("688", "689")

# tools/collectors/gtimg_quote.py parse_line() 替换 :71
    vol_raw = _num(parts[6]); amt_wan = _num(parts[37]); price = _num(parts[3])
    "volume": _to_shares(code, vol_raw, price, amt_wan),   # 统一"股",下游不再 ×100
    "volume_raw": vol_raw,                                  # 留原值审计

def _to_shares(code, vol, price, amt_wan):
    """量额自证优先(amount 元 ≈ close×volume 股 ⇒ 比值≈1;≈0.01 则为手),自证不了回落代码段规则。"""
    if vol is None: return None
    if price and amt_wan and vol > 0:
        r = vol * price / (amt_wan * 1e4)
        if 0.5 <= r <= 2.0: return vol
        if 0.005 <= r <= 0.02: return vol * 100.0
        logger.warning("gtimg %s 量额比 %.3g 无法判单位,回落代码段规则", code, r)
    return vol if exchange.is_star_market(code) else vol * 100.0

# tools/collectors/market.py:446   "volume": q.get("volume"),                       # gtimg 层已归一
# tools/collectors/market.py:114   "volume": float(x[5]) * (1.0 if exchange.is_star_market(code) else 100.0)
# tools/pipeline/intraday_screen.py:271   "volume": float(vol_hand) if vol_hand is not None else None
# market.py _assert_spot_amount_volume:全帧中位数 → 按 688/689 与其它分组各验中位数∈[0.5,2] + 逐行离群率 >1% 报警
```
同步改测试:`tests/test_market.py:145-158`(断言 `339737*100`)、`:222-232`、`tests/test_intraday_screen.py:42-49`;新增 688 用例(gtimg 行给 amount 断言不 ×100)+ 分板块断言用例。

### 2.5 回补方案对比

| 维度 | 方案 A:坏行 volume ÷100(+turnover ÷100) | **方案 B:baostock 对 618 只全量重算(推荐)** |
|---|---|---|
| 机制 | 新 `ops/fix_star_volume.py`,套 `ops/fix_turnover_unit.py:35-58` scan→dry-run→apply→原子写+meta 骨架;判据 amount 在位行比值∈[30,300]、NaN 行用邻域中位数 | `market.backfill_master(codes, start=meta.first_date)`(`market.py:318-364`,`put_master_kline` 全量覆盖);`_force_backfill_exright` 内部就是它(`master_sync.py:170`),不必经除权判定 |
| 修到什么 | 只 volume/turnover;**amount NaN 仍留** | volume/turnover/amount/OHLC 全换权威值,**顺带补齐 688 的 15 个回退日 amount** |
| 耗时 | 脚本 ~40K/0.5h;运行 <2 min | 脚本 ~20K/0.3h;运行 618 票 **3~5 min**(baostock 限速预留 15 min) |
| 风险 | 启发式;新股/停牌 volume=0 需 refuse;turnover 判据参考窗需用 08-12 前干净行 | ① **`_default_range` 缺省只回 500 自然日**(`market.py:244-251`)→ 必须传 `start=meta.first_date` 否则截掉 2018~2024;② qfq 锚点重算老 bar 微差;③ 重写 meta `source=baostock`、清 `turnover_unit_anomaly` |

**执行顺序硬约束(陷阱)**:**先修 volume 再碰 turnover**。现状下 `python -m ops.fix_turnover_unit --apply` 会把 688 09-08~17 **正确的** turnover ×100(`units.py:150-165` 掩码判异常 + `:225` 用 100× volume 验收"证明成立" → `:228` 乘 100);`ops.backfill_turnover --apply` 会用 100× volume 把 09-04/09-09 NaN 回填成 100× turnover。方案 B 整表换源天然规避。

### 2.6 下游污染清单(volume 消费者全量审计 + turnover 侧)

判读规则:窗口内**全为 100×** → 比值型指标不变、绝对阈值型崩;窗口**跨越** 08-12/13、08-13/14、08-17/18、09-04/07、09-07/08 五条边界 → 比值型 ±100× 翻转;长窗绝对量型(50 日均量、20 日均量、周量 MA8、20 日成交额)在 08-13~09-17 **持续失真无安全区**。

| 消费者 | 文件:行 | 读 | 计算·窗口 | 688 污染方向 | 严重度 |
|---|---|---|---|---|---|
| 今日选股「形态.量比」+ G4 强势旁路门 | `analysis/selection_synth.py:215-216,243`;门 `:343-344,472-473`;进 LLM prompt `:557,564,917,941` | volume | `vol[-1]/mean(vol[-6:-1])`;门 `1.0≤量比≤2.5` | 边界日 20~100(超上界)或 ≈0.02(低下界)→ 旁路两侧失效,且喂 LLM 假"爆量/极缩" | **高** |
| `technical.compute` 量比/量态 → shadow_pool 快照继承 | `analysis/technical.py:168-178`;`shadow_pool_v2.py:23` | volume | 5 日前置均量,>1.5 放量 <0.7 缩量 | 08-13/18、09-08 恒"放量";08-14/17、09-07 恒"缩量";错值固化进影子池留痕 | **高** |
| `predict.recent_volume_spikes` / `structure_anchor` 放量突破确认 | `analysis/predict.py:39-47,314,320-323,352` | volume | 同上 | 假突破盖章"放量突破"/真突破判"疑似假突破(未放量)"→ 止盈止损 `_anchor` 选错分支 | **高** |
| 形态 `_vol_confirm`(箱体/杯柄/楔形)+ `volume_ok` 硬规则 | `pattern_screener/pattern.py:26-34,54,100,127`;`screen.py:69-76,99-116` | volume | 30 日/杯长/5 日前置均量 ×1.5/1.3 | 30 日窗几乎必跨边界:末根污染 → 放量确认恒真全放行;末根干净 → 全部 688 形态被毙 | **高** |
| S02 周线放量 C1 | `pipeline/screen_s02.py:48-70,91,95-101` | volume | ISO 周量 → MA(周量,8) ×1.6 | 含 08-13 那周 ≈20×、08-18 起整周 ×100 → C1 对**全部 688 恒真**,量能门形同虚设 | **高** |
| S04 `_single` 单日放量 / `_low` 低位放量 / `_continuous` | `pipeline/screen_volume.py:58-80`(**turnover** 相邻比 1.7)/`:95-97`(10 日最大量)/`:112` | turnover + volume | 2 日 / 10 日 / 2 日 | 08-13 vs 08-12 比 100 → 假命中;09-04 vs 09-03 比 0.01 → 真放量被吞;边界日方向被强制 | **高 / 中 / 中** |
| VCP 段均量 / 量枯 | `sepa_vcp/vcp.py:77-78,139,159-162,178` | volume | 段均量 vs 50 日均量 | 50 日窗必混污染/干净 → 量枯恒真(假 VCP 放行)或恒假(真 VCP 被毙) | **高** |
| 板块角色 `build_role_features` | `sector_forecast/roles.py:82,93-95,121,145-161` | volume + amount + turnover | vol_ratio 20 日;amount/turnover 当日值进横截面 rank | 干净日 vol_ratio≈0.01 压低龙头分;08-13~09-03 turnover 566% 级 → **s_弹性 rank 被 688 霸榜**;amount NaN→0 → s_龙头/中军垫底 | **高** |
| 多策略闸门「换手极端」轴 | `analysis/multi_strategy_gate.py:184-191,308,327-345`;阈 `config/strategy.py:999`=20% | **turnover** 当日 | 无窗口 | 08-13~09-03 每只 688 候选恒"换手极端(566%≥20%)"被否决/降档;09-08 起自动恢复 | **高**(历史) |
| 反转低换手 + 现算护栏 | `pipeline/screen_reversal_turnover.py:59-61,121-131`;`strategy/reversal_turnover.py:69-99,195-226` | turnover + amount + volume | 20 日均换手/均额;现算 r=median(turnover/volume,60) | 688 被排除出低换手 Top-K;`close×volume` 兜底 ×100 成交额**绕过 5000 万流动性门**;T/V 污染区间不重合 → 双簇 MAD>0.15 → 拒绝现算 → 可能触发 `coverage_gate` 熔断 | **高** |
| `screen_revs` 流动性门 | `pipeline/screen_revs.py:86-88,94-115`(`a=close×volume` 兜底 :108-110) | amount→volume | 20 日成交额均值 | 回退日 amount NaN 必走兜底 → ×100 → 僵尸 688 全过流动性门 | **高** |
| `sector_inflection.member_pullback` 缩量回踩 | `research/sector_inflection.py:125-131` | volume | `v[t]<0.8×vma20` | 09-07 等干净日 v≈1% vma20 → "缩量回踩买点"恒真 | **高** |
| 筹码 `chip.py` 获利盘/集中度 | `analysis/chip.py`(公式待核:`grep -n "turnover\|tr\b"`) | turnover | 逐日换手推演 250 根 | 08-13~09-03 换手 100~1150%/日 → 筹码近乎全换 → 688802 09-16 获利盘 21% / 688795 33% **不可信**;最强选股 winner_rate 阈 95 对 688 失效 | **高**(待核) |
| 午盘注入 + `agent_select_*` 喂 LLM | `intraday_screen.py:271`;`experimental/agent_select_*.py:248-319` | volume | — | 当日午盘 bar ×100 叠加历史;LLM 读 688 "天量" | 高(当日)/ 中 |
| regime 量能因子 / 大盘预测 tech_vol / proxy_index | `pattern_screener/regime.py:52-71`;`market_forecast/technical_index.py:49-52`;`features.py:59` | 指数序列 / breadth total | — | 指数非个股 → **不受影响**(除非指数由 688 汇总) | 低 |
| 午盘 Q `am_pm_vol_ratio` / `liquidity_ok` | `strategy/midday_q_signals.py:127-132,183-198` | 盘中 quote(同日内比) | — | 不读主档 → 不受影响;绝对量比其它板块大 100× 若有绝对阈值则误判 | 低 |
| 回测族 | `backtest/position_backtest.py:102-105,130`;`capitulation/indicators.py:56,66`;`validate_l3_thresholds.py:61-70`;`diag_box3.py:78-86`;`research_tpsl.py:50` | volume | — | 边界日"放量滞涨"强制离场;L3 放量阈值标定被稀释/反转;08-13 后含 688 的回测结论需重跑 | 中 |
| 图表/Web 展示 | `analysis/chart.py:66`;`web/data_access.py:242` | volume | — | 688 量柱 100× | 低 |
| 最强选股 `screen_strong.signal_at` | `pipeline/screen_strong.py:149-171` | 不读三列(只 close + chip) | — | 无直接路径;经 chip 间接 | 低(直接)/高(间接) |

### 2.7 688802 沐曦 / 688795 摩尔线程 专项

- 09-15/16 入选依据:`今日选股_2026-09-16.json` / `固化_程序版双路并集`:688802 电子·板块催化·跟涨「偏弱观察」(理由:均线空头/距 60 高 −53.7%,**未提放量**,量比 1.12);688795「剔除」(财报高危红旗×1,量比 0.93);`sector_focus.json` 跟涨候选依据 pos60;`半导体多因子.json` 688795 由 rd/rev 因子入选。三个消息面 JSON **无"放量/量比/缩量"字样**。
- 量比复算(公式同 `selection_synth.py:216`,真值 = baostock):

| 日期 | 688802 存/真 | 688795 存/真 | 翻转 |
|---|---|---|---|
| 09-15 / **09-16** / 09-17 | 0.92/0.92 · **1.12/1.12** · 8.36/8.36 | 0.62/0.62 · **0.93/0.93** · 1.96/1.96 | **无**(5 日窗全在 100× 段) |
| 09-07 | 0.03 / **3.11** | 0.13 / **12.64** | **真放量被藏成极度缩量** |
| 09-08 | 3.48 / 1.89 | 28.24 / 6.73 | G4 门翻转(3.48>2.5 判爆量出局,真 1.89 应过) |
| 09-09~11 | 1.75/1.10、2.03/1.35、1.18/0.85 | 2.77/1.85、1.78/1.34、0.98/0.79 | 09-11 688802 放量→缩量;09-09 688795 G4 翻转 |
| 08-13/14/17/18 | 203/2.03、0.03/1.01、0.04/1.44、2.68/0.84 | 146/1.46、0.03/0.88、0.05/1.30、3.76/1.07 | 边界全翻 |

**结论**:09-16 大复盘两只赢家的入选/剔除结论**不因量比修正而翻转**。但 ① 摩尔线程 09-07 真实放量 12.6× 在系统里显示为 0.13——若 09-07/08 有"放量启动"类视图,688 整板块当日被系统性漏掉(待核 `grep -l "688802\|688795" data/analysis/2026-09-0{7,8}/*.json`);② 两票 09-16 获利盘 21%/33% 建立在 turnover 100× 之上,不可信,chip 公式待核后重算。

### 2.8 预期效果与核法
- 修后全量扫描:`volume×close/amount∈[0.5,2]` 的 688/689 行 = 100%(现 0%,坏 ≈13,921 行);amount NaN 回退行清零(方案 B);meta `turnover_unit_anomaly` 688 段 611 → 0;
- 今晚收盘增量(gtimg 快照)写入的 688 行比值≈1(核法 `python - <<< 读 688008 最后一行`);午盘注入 688 volume 与主档同量纲;
- 688 量比在 08-13/18、09-04/07/08 五条边界日恢复到 0.5~3 区间(以 688802/688795 复算表为回归用例);
- 新增测试:`test_gtimg_688_volume_not_scaled`、`test_spot_assert_per_board`、`test_intraday_inject_688_units`。

### 2.9 影响范围 · 工作量
- 改动:`tools/config/exchange.py`(+函数)、`gtimg_quote.py:71`、`market.py:114,446,387-417`、`intraday_screen.py:271`、3 个旧测试 + 4~5 个新用例;数据:618 只 parquet + meta 全量重写(方案 B);**不改**任何策略逻辑;
- 顺序:**先合代码修复(否则今晚 15:40 增量又写一天坏行)→ 方案 B 回补 618 只(避开 15:40~17:00 主档写窗)→ 复核扫描 → 再对 688 09-04/09-09 重跑 `backfill_turnover`(此时安全)→ 补跑 chip/量能类当日视图**;
- 工作量:代码 60~90K/1~1.5h;回补 30~40K/0.5~1h(含 5~15 min 网络);turnover 尾项 10K/0.2h;文档 15K/0.3h → **≈120~155K token / 2~3h**。


---

## H3 · 双数据根分裂

### 3.1 事实(实机盘点)

**plist 的 `WorkingDirectory` 不决定落点**——每个 wrapper 脚本自己 `cd`:14 个任务 `cd $STOCK_DAILYJOB_WORKTREE`(dailyjob),4 个板块任务(`sector_news_catalyst / sector_daily / sector_universe / sector_roster_table`)`cd $STOCK_MAIN_REPO`(主仓)。数据根 = `settings.PROJECT_ROOT = Path(__file__).parents[2]`(`tools/config/settings.py:9`)= **代码所在 worktree**,全仓 47 个文件硬编 `PROJECT_ROOT/"data"/...`,**没有统一 env 开关**。

dailyjob 侧现状(`ls -la dailyjob/data`):

| 子目录 | dailyjob | 结论 |
|---|---|---|
| `master/` `raw/` `intraday/` `backtest_local/` | **symlink → 主仓**(09-08 11:34 手工建) | 主档 kline 两侧 inode 相同 = **同一份**(H2 回补只需修一处) |
| `analysis/` | 实目录 | 分裂;靠 `mirror_to_main` 16:0x 单向拷 `analysis/<D>/` + `*forward_scorecard*.csv` + `sync_receipts/<D>.json`(`tools/sync/mirror_to_main.py:105-123`,由 `upload.py:291` 触发,每天两遍) |
| `breadth/` | 实目录 | **零回写**:主仓缺 09-11/14/15/16,dailyjob 缺 09-04/07/08(worktree 09-08 才建) |
| `shadow_forward/` | 实目录(evidence_09-15/16/17、sentiment/inflection/macro_avoid/veto_track/sector_news 五个 shadow 产物) | **只在 dailyjob**;5 个 shadow 任务用 cwd 相对路径写(`run.py:1492-1502 Path.cwd()/data/shadow_forward`、`research/*.py DEFAULT_OUT_DIR`) |
| `sync_receipts/pool_ack.json` `pool_pending.json` | 09-17 | 主仓停 09-10,未回写 |
| `logs/` | python 侧 `LOG_PATH=PROJECT_ROOT/logs`(`market_breadth.py:91`、`intraday_watch.py:226` 等) | 主仓 `logs/` 天然没有;launchd stdout/stderr 在 `~/.local/state/stock/` |
| 主仓 only | `signals/capitulation/`、`breadth_series/`(09-14 手跑)、`sector_news/`、`sector_roster*/`、`nextday_scorecard.csv` | 主仓 cwd 任务/手跑产出,dailyjob 看不到;`sector_news_forward_shadow`(dailyjob)靠 `market_step.resolve_analysis_file`(:24-33)"本仓→主仓"回退才读到 `sector_focus.json`——第 4 套数据根解析 |

另两处漂移(非本轮阻塞,列入纠偏):
- 实机 8 个 plist 的 `ProgramArguments` 指向**主仓工作树**的 `ops/launchd/<x>.sh`,而仓内版指向 `~/.local/state/stock/bin/stock-launchd-bootstrap.sh`(③b 部署卫生未上线:`provision_deploy.sh` 未跑、deploy worktree 不存在)→ wrapper 用主仓版本、python 用 dailyjob(每轮 `reset --hard origin/main`)= **两个版本源**;
- `tools/scheduling/tasks.yaml` 与实机 6 处相反(4 个 sector/shadow `enabled:false` 实为 live;`strong` enabled 实已 retired;`intraday_noon` slot 1131 vs 实机 1145)。

### 3.2 三方案比较

| | A. 单一数据根(env 注入,Phase-2 E0~E3) | B. dailyjob 唯一写者 + 扩 mirror 白名单 | **C2. 补齐子目录 symlink(推荐止血)** |
|---|---|---|---|
| 做法 | `settings.data_root()` 收编 47 处引用,dailyjob 经 env 把根指主仓 `data/`(设计见 `docs/计划/2026-09-13_Phase2数据根解耦设计.md`)。**不能**把 `STOCK_DAILYJOB_WORKTREE` 直接指主仓——脚本会 `reset --hard`(`pull_refresh.sh:37`) | `mirror_to_main.py:105-123` 白名单加 `breadth/ shadow_forward/ signals/ breadth_series/ sync_receipts/pool_*.json`;breadth 15:05 产物要等 16:05 才到主仓,需在 `market_breadth.sh` 及 5 个 shadow sh 各加一次回写 | dailyjob 侧:`rsync -a --ignore-existing data/{breadth,shadow_forward,sync_receipts} 主仓/data/` → `rm -r` → `ln -s 主仓对应目录`;`signals/ breadth_series/` 反向同法。全部 gitignore(`.gitignore:8,60,72,73`)**除 `data/breadth/2026-09-03.json` 是 tracked**,需先 `git rm --cached` + `.gitignore` 加 `data/breadth/`(1 个 commit) |
| 改动 | 47 文件 + settings + repo | mirror +40 行 + 6 个 sh 各 1 行 + 测试 | **代码 0 行**,运维 6 条命令 + 1 commit |
| 风险 | 高(改生产读写路径;主仓工作树更脏) | 中(每新增产物目录都要记得补白名单——本轮已漏 5 类) | 低(master/raw 同模式已稳跑 9 天);**待核**:`git reset --hard` 对"gitignore 目录被 symlink 替代"无感——scratch 克隆里 `ln -s … data/shadow_forward && git reset --hard` 验证 |
| 回滚 | 不可瞬回 | 删白名单行 | `rm symlink && mv 回来`,分钟级 |
| 工作量 | ~300~470K / 10~13h | ~40~60K / 1.5~2h | **~20~30K / ~1h** |

**推荐:C2 立即做 + Phase-1 mirror 保留 + Phase-2(A)按原文档分阶段根治。** 不推荐 B(把白名单债务长期化)。附带纠偏:跑 `provision_deploy.sh` 并按 `docs/参考/部署卫生_launchd装载runbook.md` 重装 8 个 plist(需用户本人 `launchctl`);tasks.yaml 六处对齐实机。

### 3.3 回补(可立刻做,不依赖方案)

| 方向 | 文件 | 可信度(已核 json) | 动作 |
|---|---|---|---|
| dailyjob → 主仓 | `breadth/2026-09-11/14/15/16.json` | coverage 0.99982(缺 02513 港股一只,09-08 起每日同)、degraded=false、drift 5~14 s、mode=realtime | `cp -p` 直接拷 |
| 主仓 → dailyjob | `breadth/2026-09-04/07/08.json` | coverage 1.0/1.0/0.99982、drift 2~6 s | `cp -p` 反向拷 |
| 09-17 双版本 | 主仓 15:12 手跑 vs dailyjob 15:05 定时 | `mean_pct/median/up/down/net_breadth/limit_up/coverage` 逐位相同,只差 captured_at/drift(466 vs 9)/quote_time | 数值等价,保留 dailyjob 版 |

### 3.4 预期效果与核法
- 主仓与 dailyjob `data/breadth/` 文件集合一致(11 个交易日 09-03~09-17);`equal_weight_index`(链①)在主仓可算出连续净值 → H4 链① 不再断;
- `ls -la dailyjob/data/` 8 个子目录中 7 个为 symlink(`analysis/` 除外);`shadow_forward/` 在主仓可见,web / position_ledger 可读;
- 核法:`for d in breadth shadow_forward sync_receipts; do readlink dailyjob/data/$d; done`;`diff <(ls 主仓/data/breadth) <(ls dailyjob/data/breadth)` 为空。

### 3.5 影响范围
- 不改代码;改 dailyjob 工作副本的目录拓扑 + 主仓 `.gitignore` 一行 + untrack 1 个文件;
- 风险点:C2 执行窗口要避开 15:05 breadth / 15:40 pullrefresh / 20:00~21:15 shadow 五连(建议 09:00~11:00 或 22:30 后);`reset --hard` 兼容性待核(见 §3.2)。

---

## H4 · α 基准 market_ew 未落盘

### 4.1 诊断(修正 A4 的两个前提)

| A4 假设 | 实测(主档 kline 回算 vs breadth json,逐日,scratchpad `h4/ew_recompute_20260801_20260917.csv`) |
|---|---|
| 三条链"数学不等价、差多大待测" | 链②(kline 全A `pct_chg` 算术均值,dropna + ±30 截尾)与 breadth 实时 `mean_pct` 在**北交所主档健康的 8 天里逐日偏差 ≤0.001pp**;三个偏差日(09-03 +0.117 / 09-04 −0.037 / 09-09 +0.058)**全是北交所主档缺行或 pct_chg=NaN 的数据缺陷**(09-03 kline 无 BJ 行、09-04/09-09 BJ 341/344 行 pct=NaN),不是定义差。A2 chip 的"≤0.11pp"就是 09-03 那一天 |
| 链①缺日只是"回退幸存者基准" | 更糟:`shadow_forward.py:244-245 _ew_bench_return` 只判数组越界不判日历连续 → **breadth 断档时 "T+1" 基准悄悄变成跨多日收益**(主仓根 09-10→09-17 当 T+1;dailyjob 根 09-03→09-09 当 T+1),已实测复现 |

三条链定义差异(真正的口径差只有一处:IPO/复牌首日):

| | 链① `equal_weight_index.py:55-64` | 链② `research/finval/build_market_index.py:38-46` | 链③a 实时 `pipeline/market_breadth.py:289-314` | 链③b 历史 `market_forecast/breadth.py:compute_breadth` |
|---|---|---|---|---|
| 均值 | 复利 breadth.mean_pct(本身算术均值),BASE 1000 | 算术均值,dropna,**`[-30,30]` 截尾**(:41) | 算术均值,`nansum/total` NaN 计分母 | 同③a,不截尾 |
| universe | = breadth | `glob master/kline/*.parquet`(:23)→ **含 5 位港股 02513** | `list_master_codes()` 含 ST/北交所 | 同 |
| IPO 首日(+146%~+662%) | — | 截掉 | **天然不含**(15:05 新票未入主档) | **含** → 系统性偏高 0.02~0.21pp(08-11 −0.208) |
| 落盘 | 不落盘,每次重读全部 json | `analysis/backtest/finval/market_ew.parquet`(**从未生成**) | 每日 json | `data/breadth_series/capitulation_breadth.parquet`(仅主仓,停 09-11) |

**为何从未生成**(`build_market_index.py`):全文无 argparse,`main()` 无参数(:22)→ `--data-root` 根本不被解析;`ROOT` 由 `__file__` 硬编码为**代码仓根**(:17-19);`pull_refresh.sh / tasks.yaml / run.py / plist` **零处**调度;唯一入口 `review/build_grand_review.py:80 --build-ew` 手动 flag,且 `model_a.ensure_ew(data_root)`(:29-46)调 `build_market_index.main()` **不传 data_root** → 写到代码根、再从数据根读 → `{}`。

**消费者**(读什么 / 缺失回退 / 问题):`shadow_forward.py:292,237-268`(breadth 净值;缺→forward_scorecard 全样本均值 `bench_src="scorecard代理"`;**跨日 bug**)、`position_ledger.py:290`(主仓 breadth 单日点位)、`web/data_access.py:1575,1442`(主仓 breadth 当日 mean_pct)、`research/sector_news_forward.py:52,185-195,259-270`(finval parquet,缺→excess 恒 null)、`review/model_a.py:29-46,71`(同,→ 09-16 大复盘 78 行 α null)、`research/finval/build_panel.py:26,65`(同,无存在性检查直接抛)、`intraday_review.py:144` / `intraday_cadence.py:181`(快照即时算,**应保留**)、`market_forecast/features.py:52-62`(链③b,不动)。

### 4.2 落盘方案

**路径 `<data_root>/analysis/backtest/market_ew.parquet`**(去 `finval/` 子目录:它已是项目级基准;与 `forward_scorecard.csv` 同目录同根;旧路径 loader 兼容读一轮)。不放 `data/master/`:master 是采集层原始 bar 契约,不混派生物。

| 列 | 含义 |
|---|---|
| `date` | YYYY-MM-DD |
| `ew_ret_pct` | 全A等权日收益 %(算术均值) |
| `ew_index` | 复利净值,BASE 1000 锚序列首日 |
| `n` / `n_listed` / `n_trunc` | 入均值样本数 / 当日有 bar 只数(审计 BJ 缺陷)/ 被截尾行数 |
| `source` | `kline_master` / `breadth_realtime`(当日临时行) |
| `universe_def` | 常量 `master_kline;code=^\d{6}$;incl_ST;incl_BJ;excl_nan;excl_abs_pct_gt_30` |
| `built_at` | ISO 时刻 |

口径:沿用链②(dropna + ±30 截尾;IPO 首日不可买入,截尾经济上正确)+ `^\d{6}$` 堵港股;**不**采用 NaN 计分母。历史回算 2018 起全量(实测 5566 文件 14.8s),**每日全量重建**(幂等、自愈 BJ 回填)。

**三链收口**:新模块 `tools/analysis/market_ew.py`:`build(data_root, out)` / `load(data_root, breadth_fallback=True)`(parquet 主 + 当日缺则用 `breadth/<D>.json` mean_pct 补一行标 `source=breadth_realtime`)/ `level_at` / **`bench_return(df, d0, N)` 按交易日历取 D+N,任一端缺 → None,绝不跨日**;`build_market_index.py` 改薄包装(加 argparse);链① `equal_weight_index.py` 降为"当日/盘中回退源";链③a/③b 不动。改读者:`shadow_forward.forward_alpha`(**去掉 scorecard 回退**,缺基准 → `bench_src="无基准"`、α=None)、`sector_news_forward._load_ew`、`model_a.ensure_ew`(传 data_root)、`build_panel.MKT`、`position_ledger.level_at`。

**每日挂载点**:`ops/launchd/pull_refresh.sh` ①.7 turnover 兜底(:107)之后、② screenall(:108)之前插 ①.8:

```bash
echo "-- ①.8 全A等权基准落盘(主档全量重建 ~15s;供 ②.5 记分卡/④ 影子跑/大复盘 α 同源) --"
"$PY" -m tools.analysis.market_ew build --data-root "$REPO/data" \
    || echo "!! market_ew 落盘失败(不阻断;消费者回退 breadth 当日截面,再缺则 α=null)"
```
`tasks.yaml` 加 `id: market_ew, depends_on: [remote_fetch]`。时序:breadth 15:05 → ① 采集 ~73min → ①.8 ~17:00 含当日行 → ②.5/④ 读到。

**BENCHMARK_NOTE 改为**:"α基准=全A等权日收益(主档 kline 全A pct_chg 横截面算术均值;含ST/北交所,剔除当日无涨跌幅行与|pct|>30 的 IPO/复牌行;落盘 data/analysis/backtest/market_ew.parquet,盘后随主档全量重建);当日主档未推进时回退 data/breadth 收盘实时截面(同口径,历史偏差≤0.001pp,标 source=breadth_realtime);T+N 基准按交易日历取 D+N,任一端缺 → 该日基准 null、α 不计(不再回退 forward_scorecard 全样本均值);研究用非投资建议"。

Patch 草案(builder/loader/bench_return 核心代码、shadow_forward diff、sector_news_forward/model_a/build_panel/position_ledger 改点)已由子调研写出,执行窗口按本节实现即可;测试 4 条:NaN/+150%/5 位码剔除计数;`bench_return` 中间缺日返回 None(锁"绝不跨日");breadth 回退行不覆盖 kline 行;`ensure_ew(tmp_root)` 构建后同根可读。

### 4.3 预期效果与核法
- `data/analysis/backtest/market_ew.parquet` 存在,2026-07-31~当日无缺日,`n_listed` 5202→5565;
- 09-16 大复盘 78 行 `alpha_d1` 非 null;`sector_news_forward.excess_d1` 非 null;shadow_forward evidence `bench_src` 全为 `market_ew`(无 `scorecard代理`);
- 核法:`market_ew.load()` 与 dailyjob breadth 11 日逐日 |差| ≤0.001pp(BJ 缺陷日除外,待 H2/BJ 回填后归零);`tests/test_market_ew.py` 4 条绿。

### 4.4 影响范围 · 工作量 · 待核
- 新增 1 模块 + 改 6 个消费者 + 1 行 shell + tasks.yaml + 4 测试;不改 breadth 生产、不改 market_forecast;
- 工作量 **~0.3~0.45M token / 3.5~6h**(builder+测试 1.5~2.5h;消费者改读 1~2h;实跑验证 0.5~1h;文档 0.5h);
- 依赖:H3 回补 breadth 后主仓侧 web/position_ledger 才能读到回退行;BJ 主档 09-04/09-09 NaN(疑 `fallback_advance` 未算 pct,核法 `grep fallback_advance ops/remote_fetch.py` + 看 920xxx meta.source)修好后三日偏差自动消失;
- 待核:09-10 breadth universe_n=5561 vs kline listed 5560(IPO 301689 15:05 未入主档 + 1 只停牌?);除权日 pct 口径(主档回填行是原始价 `close.pct_change()`,gtimg 是官方前收口径,量级 <0.01pp)。


---

## H5 · 记分卡单位 / 入场口径不一致

### 5.1 口径总表(代码实证,单位 pct=已×100 百分数 / dec=小数)

| # | 产物 | 生产者 文件:行 | 收益列 | 单位 | 入场 | 出场·持有期 | α 基准 | 成本 | 口径元字段 | 程序 reader |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `backtest/forward_scorecard.csv`(7810×35) | `tools/backtest/forward_scorecard.py:366` `(close[idx+N]/close[idx]-1)*100` | `r_1/5/10, hit_*` | **pct** | close_T(信号日收盘) | close[T+N];1/5/10 | 无 | 无 | 无 | shadow_score:16-37、shadow_forward:264-295(回退基准)、run.py:1502、4 个 artifacts 脚本、mirror_to_main:35 |
| 2 | `backtest/lhb_forward_scorecard.csv`(430×15,止 09-11) | `lhb_forward_scorecard.py:134` `cl[j]/entry_px-1` round4;入场 :126-128 | `r_1/r_5, underperf_*` | **dec** | open_T1 | **锚在信号日 idx**:h=1 实为 T+1 开→收日内、h=5 实持 4 日 | 无 | 无 | 无 | **零 reader** |
| 3 | `backtest/nextday_scorecard.csv`(2 行) | `nextday_scorecard.py:58` `*100` | `avg_abs_return, avg_alpha` | pct | limit_P(P_entry 外部传入) | D+1 收 | 全A等权(算法在 SKILL,仓内无) | 无 | 无 | 零 |
| 4 | `shadow_forward/evidence_<D>.json` | `shadow_forward.py:92` `*100`;基准 :237-250 breadth 链/回退 :264-267 | `labels.r_1/r_5`(α 仅内存) | pct | close_T | close[T+N] | 全A等权(breadth)→回退 #1 均值,带 `bench_src` | 无 | `version/horizons/benchmark_note`,缺 unit/entry_rule | run.py:1488-1529 |
| 5 | `grand_review_scorecard.csv`(**未生成**)+ 大复盘 md | `sector_news_forward.py:243-255` `*100`;`review/model_a.py:71-113`;列 `render.py:16-23` | `r_d1/r_d2/r_exit/alpha_*` | pct | limit_P(Model-A) | D+1 / 条件 D+2 | `finval/market_ew.parquet`(**不存在→null**,H4) | 无 | 仅 md 文字 | 零 |
| 6 | `intraday_overnight_scorecard.csv`(未生成) | `review/intraday_cadence.py:167` | `r_overnight/alpha_overnight` | pct | close_T | D+1 11:30 快照 | noon 等权 | 无 | `sell_src/degraded` | 零 |
| 7 | 午盘复盘 md | `pipeline/intraday_review.py:127` | `下午涨跌%/下午α` | pct | snap_1130 | 当日收 | 快照池下午等权 | 无 | 无 | 零 |
| 8 | `positions.json`(未生成) | `position_ledger.py:121-133` | `pnl_pct/alpha_pct` | pct | inject | inject | `equal_weight_index.level_at` | 无 | `version` | intraday_watch(只取 code) |
| 9 | `backtest/eval_v3.json` | `eval_v3/prices.py:98` `*100`;入场 :47-61 | `r, 均值%, 超额%` | pct | open_T1 | close[T+h](锚信号日) | 抽样宇宙等权 T+1 | 无 | 中文串,无机读键 | 零(模块被 #2/#12/#13 复用) |
| 10 | `strategy_scorecard.json` + `docs/策略成绩报告.md` | `strategy_scorecard.py:216-218` **`entry=close[idx]`** | `平均实现收益%` | pct | **close_T**(A4 字典 :156 归"T+1 开盘"**有误**) | close[T+h] | 方向命中超额(vs P_up),非收益 α | 无 | 中文串 | 零;md 与 eval_v3 `cli.py:27` **双写同路径** |
| 11 | `expert_scorecard*.json` | `expert_scorecard.py:57` | `平均收益%` | pct | close_T | close[T+h] | 方向超额 | 无 | 中文串 | 零;零测试 |
| 12 | `lhb_veto_lab.json`→`lhb_dose_calibration.json` | `lhb_block_lab.py:176`;成本 :48 `COST_RT=0.002` | `mean_stk_ret/net_excess` | **dec** | open_T1 | close[T+h] | 沪深300 bootstrap | **0.2% 往返** | `window/bench/cost_rt`,缺 unit | `lhb_dose_lab.py:156`(只用符号+p 值) |
| 13 | `lhb_dose_replay.json` | `lhb_dose_replay.py:104` round4 | `r_1/r_5` | **dec** | open_T1 | close[T+h] | 无 | 无 | `horizons/axis_used` | 零 |
| 14 | persistence_result.json(仓内不存在) | `backtest_pead.py:157` | `均值收益/均值Alpha` | **dec** | close_T1 | close[E+N];5/10/20/60 | 沪深300(不落盘) | 无 | 无 | 零 |
| 15 | `<D>/backtest.json`(样本恒 0) | `event_study.py:65` round6 | `平均收益/平均Alpha` | **dec**(胜率也 dec) | close_T1 | close[E+N] | 沪深300 | 无 | **最全**(窗口/基准/口径/免责),缺 unit | 零 |
| 16 | `screen_forward_common.py`(库) | `event_study.py:65`(dec 存)/`:254-255`(×100 显) | 内存 | dec 存 pct 显 | close_T | 1/5/10/20 | HS300 | 无 | — | backtest_sepa_vcp/trend_template/conditional |

**实测**(主仓 CSV):forward r_1 std 3.87 vs lhb 0.048(80×);同 (date,code) 130 行配对相关 0.826、**中位比 103×**(≠100 正是 close_T vs open_T1 + h=1 日内口径差)。全仓**无程序把两表数值混算**——唯一同时触碰的是 `mirror_to_main.py:35` glob 字节拷贝;风险在人/Agent 横向对读。

**顺手核出的具体缺陷**

| 编号 | 问题 | 证据 | 影响 |
|---|---|---|---|
| F1 | `diag_recall.py` 把 pct 当 dec:`WIN_CUT=0.05 # r_1>=5%`,实为 0.05% | `docs/计划/2026-09-14_召回通路_artifacts/diag_recall.py:39,52` | 09-14 召回 AB 报告赢家/漏网统计失真,需重算 |
| F2 | veto_lab 结论键名标 `%` 值却是小数,被 `lhb_dose_lab.py:204-208` 拼进文案 | `lhb_veto_lab.py:186-187` | 人读误判量级 |
| F3 | A4 字典把 strategy_scorecard 归 T+1 开盘,代码是 close_T | A4 :156 vs `strategy_scorecard.py:216` | 改文档 |
| F4 | 龙虎榜四件 + eval_v3 出场锚信号日非入场日 → h=1 是日内 | `lhb_forward_scorecard.py:126,132` 等 | 跨族对读把"日内"当"1 日" |
| F5 | `docs/策略成绩报告.md` 被两个入场口径不同的生产者双写 | `strategy_scorecard.py:114` / `eval_v3/cli.py:27` | 报告口径随谁最后跑漂移 |
| F6 | 磁盘 eval_v3.json 旧 schema(`类型: ranking`,含 3 日) | `eval_v3/schema.py:41` | 重跑对齐 |
| F7 | lhb_forward 测试只验符号 | `tests/test_lhb_dose_calibration.py:157-160` | 改单位测试不红 |

### 5.2 统一口径规范(提案)

**单位迁移两法比较**

| | A. 改列名 `r_1→r_1_pct` | **B. 保留列名 + 强制 pct + 加 `unit` 列(推荐)** |
|---|---|---|
| 破坏面 | 大:#1 的 6 个 reader 全按 `"r_1"` 硬取键;~8 个测试 | 小:pct 产物 reader 零改动;dec 产物(#2/12/13/14/15)**恰好零数值 reader** |
| 改动 | ~15 文件 + 8 测试 + 历史 CSV rebuild | 生产者 5 处 ×100 + 测试 3 处;`COST_RT` 0.002→0.2 |
| 防混 | 列名自带 | 靠 `unit` 列 + 公共读取器 `scorecard_io.load()`(读到 `unit=="dec"` 自动 ×100,缺列按文件名白名单推断并 warn) |

**口径元字段**(CSV 每行常量列 / JSON 顶层键;常量放 `tools/backtest/scorecard_schema.py`,含 `stamp(df, **meta)` + `validate`)

| 字段 | 枚举 |
|---|---|
| `unit` | `pct` |
| `entry_rule` | `close_T` / `open_T1` / `close_T1` / `limit_P` / `model_a` / `snap_1130` / `inject` |
| `exit_rule` | `close_T+N`(锚信号日)/ `close_E+N`(锚入场日)/ `snap_1130_T1` / `close_T` / `cond_d2` / `inject` —— **必须区分锚点(F4)** |
| `horizon` | int(JSON 给 `horizons[]`) |
| `cost_bp` | 0 / 20 |
| `alpha_bench` | `none` / `hs300` / `market_ew_parquet` / `breadth_ew` / `noon_ew` / `universe_ew_T1` / `p_up_dir`(方向命中超额,非收益 α)/ `scorecard_proxy` |
| `schema_version` | str |

**逐产物迁移改动点**:#1 `forward_scorecard.py:162-169` 加 5 常量列(幂等全量写,下次 pull_refresh 自动带列,无需 rebuild);#2 `:134 *100` + `:51-52 _COLS` 加元字段 + **一次性 --rebuild**(增量合并会让 dec 旧行与 pct 新行混表);#3 `nextday_scorecard.py:33-42`;#4 `shadow_forward.py:168-181` rec 加键;#5 `render.py:16-23` + `model_a.py:74-84` 透传 `LIMIT_RULE`;#9 `aggregate.py:217-219` 加机读键;#10 `:312-314` 加键 + `:114` 改 md 默认路径解 F5;#12 `lhb_block_lab.py:176,137 *100`、`:48 COST_RT=0.2`、`lhb_veto_lab.py:186-187` 修 F2;#13 `:104,:195 *100`;#14/#15/#16 `event_study.py:65,85,99 *100` 且 `screen_forward_common.py:254-259` 去 `*100`(共享内核,单独一步)。

**顺序**:① schema/io 公共层 + pct 三件加列(#1/#3/#4,零风险)→ ② 龙虎榜 dec 四件一起改一起 rebuild(#2/#12/#13)→ ③ 评测 JSON 三件加机读键 + F5/F6 重跑 → ④ event_study 内核 → ⑤ F1 重算、F3 改字典、开发日志。

### 5.3 预期效果与核法
- `ls data/analysis/backtest/*.csv` 每张都有 `unit=pct` 与 `entry_rule` 列;`scorecard_io.load()` 对任一产物返回同量纲 DataFrame;
- 核法:`pd.read_csv(lhb).r_1.abs().gt(1).mean()` 从 0% 升到 ~70%(与 forward 同分布);同 (date,code) 配对中位比从 103× 降到 ~1.03×(仅剩入场口径差);新增 `tests/test_scorecard_schema.py` 锁 unit/entry_rule 枚举。

### 5.4 影响范围 · 工作量 · 优先级
- 改动 ~15 个生产者文件 + 新建 2 个公共模块 + ~8 个测试;生产决策链**不读任何 dec 产物数值**(lhb_forward 零 reader;veto_lab 唯一 reader 只看符号/p 值;`config/strategy.py:718,729` 是注释),故**不必今晚修**,周内一个 feat 分支。
- 工作量:~500K token / 5~6h(可拆两轮:①②一轮,③④⑤一轮)。
- **今晚顺手 10 分钟**:在 A4 字典 :156 改对 F3,并在两张 CSV 条目里明写"forward=pct、lhb=dec、中位比 103×",防今晚复盘 Agent 横向对读踩坑。
- 待核:(a) nextday `avg_alpha` 基准算法在 eod-review SKILL 内(找 `--picks` JSON 样本);(b) `backtest_pead.py` 自身产物 reader(`grep -rn backtest_pead tools/`);(c) eval_v3 磁盘版生成参数。


---

## 6. 优先级、依赖与工作量总表

### 6.1 依赖图

```
H1-F2 ulimit ─┐
H1-F3 并发回4 ─┼─► H1-F1 _safe 包裹 ─► 今晚 15:40 收盘闭环跑通 ─► (可选)09-17 回补(须 15:40 前)
H2 代码修复 ──┘        │
H2 主档回补(方案 B,避开 15:40~17:00)──► 688 turnover 尾项 ──► chip/量能视图补跑
H3 breadth 四日 cp ─► H4 market_ew 落盘(挂 pull_refresh ①.8)─► shadow/大复盘 α 非 null
H3-C2 symlink(22:30 后或 09:00~11:00 窗口)
H5 schema/io 层 ─► 龙虎榜 dec 四件 ─► 评测 JSON ─► event_study 内核
H1-F4/F5 根治(client 复用 + 全局 in-flight 闸 + 可观测)
```

### 6.2 总表(agent 视角)

| 序 | 项 | 必须今晚(09-18 15:40 前)? | token(in+out) | agent 工时(含审阅) | 备注 |
|---|---|---|---|---|---|
| 1 | H1-F2 `ulimit -Sn 65536` 进 `pull_refresh.sh`/`intraday_screen.sh`/`lib` + `run.py` rlimit 兜底 | **是** | 5K | 15 min | 1 行 shell + 5 行 python |
| 2 | H1-F3 `ENRICH_WORKERS=4 LLM_EXTRACT_WORKERS=4 CANDMSG_WORKERS=6` 进 plist/sh env | **是** | 3K | 10 min | 只改 env,config 已支持 |
| 3 | H1-F1 `run.py:1271,1274-1283` 八处 `_safe` + `_GAP_ALARM` marker + 1 个降级测试 | **是** | 15~25K | 30~45 min | 让崩变降级 |
| 4 | H2 代码修复(`exchange.is_star_market` + gtimg 归一/自证 + 三处去 ×100 + 分板块断言 + 测试) | **是** | 60~90K | 1~1.5h | 否则今晚再写一天坏行 |
| 5 | H2 主档回补 方案 B(618 只 baostock 重算,`start=meta.first_date`)+ 复核扫描 | **是**(避开 15:40~17:00) | 30~40K | 0.5~1h | 今晚金字塔 v1 的 688 量能才是真的 |
| 6 | 09-17 收盘闭环回补(`_as_of` 读 `STOCK_AS_OF` 一行 patch + 在 dailyjob 重跑 screenall/forward/shadow + mirror) | 可选,**仅 15:40 前有效** | 10K + 网关 LLM 0.6~1.0M | 1.5h 值守 | 来不及则与 09-14~16 同处理 |
| 7 | H3 breadth 四日 `cp -p` 双向回补 | 建议今晚(10 min) | 2K | 10 min | H4 前置 |
| 8 | H5 今晚顺手项:A4 字典 :156 改 F3 + 两张 CSV 条目写明单位 | 建议今晚(10 min) | 10K | 10 min | 防复盘 Agent 横向对读踩坑 |
| 9 | H2 turnover 尾项(688 09-04/09-09 `backfill_turnover` 重跑)+ chip/量能视图补跑 | 明日 | 10~20K | 0.5h | 必须在 5 之后 |
| 10 | H4 `market_ew.py` + 6 消费者改读 + pull_refresh ①.8 + tasks.yaml + 4 测试 + 实跑验证 | 周内(优先,D+2 回填/大复盘 α 等它) | 300~450K | 3.5~6h | 依赖 7 |
| 11 | H3-C2 symlink(breadth/shadow_forward/sync_receipts/signals/breadth_series)+ untrack 1 文件 + 待核 `reset --hard` 兼容 | 周内 | 20~30K | 1h | 运维为主 |
| 12 | H1-F4/F5 根治(client 缓存 + close + 全局 in-flight 闸 + fd 可观测 + 3 测试)后再逐步把并发调回 | 周内 | 100~120K | 2.5~3h | 做完才允许并发回 10 |
| 13 | H5 统一口径(schema/io 层 → 龙虎榜 dec 四件 rebuild → 评测 JSON → event_study 内核 → F1 重算) | 周内(可拆两轮) | ~500K | 5~6h | 生产链不读 dec 数值,不紧急 |
| 14 | H3 Phase-2 数据根根治(E0~E3)+ 部署卫生 ③b + tasks.yaml 六处对齐 | 下周 | 300~470K | 10~13h | 原文档已有设计 |
| | **今晚必修合计(1~5)** | | **~115~165K** | **~2.5~3.5h** | 可两窗口并行:H1(1~3)与 H2(4~5)解耦、不改同一文件 |
| | **全部合计** | | **~1.5~2.0M** | **~30~38h** | |

### 6.3 建议派工切分(解耦、可并行)
- 窗口 A「H1 止血」:项 1~3(改 `run.py` + `ops/launchd/*.sh` + plist env);完工标准:本地 `pytest tests/test_run_*`(新增降级测试)绿 + `bash -n` 通过;由统筹在 15:40 前合 main 并 `launchctl kickstart`(或等自然触发)。
- 窗口 B「H2 修复+回补」:项 4~5(改 `tools/collectors/*` + `tools/config/exchange.py` + `intraday_screen.py:271` + 测试;回补脚本只读主仓 meta 定 `start`,写主仓 `master/kline`,**必须避开 15:40~17:00**);完工标准:全量扫描 618 只比值≈1、`pytest tests/test_market.py tests/test_intraday_screen.py` 绿。
- 窗口 C(明日起)「H4 基准落盘」:项 7 + 10;窗口 D「H5 口径」:项 13;窗口 E「H3 运维」:项 11 + 14。
- 09-17 回补(项 6)只有统筹在 09-18 上午拍板才做,否则跳过。

### 6.4 待核汇总(本轮未能直接证实,附核法)
1. launchd 下 python 实际 `RLIMIT_NOFILE` 软限=256 —— `pull_refresh.sh` 头加 `ulimit -Sn` 打印一次(H1)。
2. 09-07/08-14/08-17 688 行为何正确(疑命中 sina/baostock)—— `grep "命中源\|回退" ~/.local/state/stock/*.err.log | grep 2026-09-07`(H2)。
3. `688981.parquet` 一次 "magic bytes not found" 是否为并发原子写窗口 —— 收盘窗口外重读全量统计 `ArrowInvalid`(H2)。
4. `chip.py` 筹码推演公式是否用 turnover —— `grep -n "turnover\|tr\b" tools/analysis/chip.py`(H2)。
5. 09-07/08 是否有"放量启动"类视图漏掉 688 —— `grep -l "688802\|688795" data/analysis/2026-09-0{7,8}/*.json`(H2)。
6. C2 symlink 与 `git reset --hard` 兼容 —— scratch 克隆验证(H3)。
7. raw `lhb` 09-14~17 15:47~15:55 的写者(非 screenall)—— `grep -rn "put_raw(\"lhb\"" tools/`(H1)。
8. BJ 主档 09-04/09-09 pct_chg NaN 写入路径 —— `grep fallback_advance ops/remote_fetch.py` + 920xxx meta.source(H4)。
9. nextday `avg_alpha` 基准算法(在 eod-review SKILL)、`backtest_pead` 产物 reader、eval_v3 磁盘版参数(H5)。

### 6.5 方法与来源
- 日志:`~/.local/state/stock/pull_refresh.log`(80 MB,行号引用见 §1)、`intraday_screen.log`、`market_breadth.log`;
- 代码:本 worktree(= origin/main `8c71aa4`);数据:主仓 `data/` 与 dailyjob `data/`,全部只读;实验产物在会话 scratchpad(`h4/ew_recompute_20260801_20260917.csv`);
- 4 个并行只读子调研(H2 含 2 个二级审计)+ 本窗口 H1 主调研;本文不含凭证、协作暗号、网关品牌名。

