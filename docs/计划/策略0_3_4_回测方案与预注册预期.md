# 策略 0 / 3 / 4 回测方案 + 预注册预期

> 日期:2026-08-13 · 状态:方案待审(稳则即测)· ⚠️ 非投资建议
> 纪律(承接 S01 教训 + 本项目约法):**先探查用途 → 定回测口径 → 测前把预期钉死(预注册)→ 再跑**。
> 预注册的意义:结果出来前先写下"我预测什么、什么算成功/失败、若失败最可能因为什么、预设怎么调"。这样调参是**验证假设**,不是事后找理由(防 p-hacking)。

---

## 0. 三个策略的用途(已读代码确认,不是猜)

| 策略 | 代码 | 本质用途 | 回测类型 |
|---|---|---|---|
| **3 箱体形态** | `screen_box` | 窄幅箱体 **AND 放量突破箱顶** → **单日触发的买入信号** | **信号型**(同 S01/S02:进场→离场/前瞻) |
| **4 动量组合(A腿)** | `screen_momentum` | 加权对数动量打分 → R²闸门 + 低通信号 → **按分排序取 TopK** | **排序型**(分层/IC/TopK 组合) |
| **0 全A合议** | `screen_council` | 8 专家打分 → **综合分降序取 Top20**(打分排座次,非离散买卖) | **排序型**(IC + 分层多空 spread) |

> 关键区分:**3 是"买不买"的信号,0/4 是"谁更强"的排序**。信号型看胜率/盈亏比/Alpha;排序型看 IC(预测力)和头尾分位收益差,两者回测口径不能混(混了就是又画错靶)。

---

## 1. 统一方法(三者共用,防未来函数)

- **样本**:随机全A ~700 只(主档随机抽样,破"偏成长"偏差;复用本轮 `backtest_predict._sample_universe`)。要更硬时扩样本或 literally 全A。
- **基准**:沪深300(同 S01/S02,`collectors.index`),算相对 Alpha。
- **无未来函数**:逐历史日 t **只用 kdf.iloc[:t+1] 重放筛选**(三个 screener 均已声明截至当日判定);进场锚 **t+1**,前瞻收益只取 t+1 及之后。**红线**:构造未来极端数据不得改变 t 时刻信号(单测锁死,同 `backtest_predict` / engine)。
- **持有期**:前瞻 5 / 10 / 20 交易日(交易日非自然日)。
- **口径诚实**:信号型先做"信号日收盘机械基线"(无择时无调仓,衡量信号原始质量),明确标注**非最终买法**(S01/S02 已立此规矩)。

---

## 2. 各策略回测口径

### 2.1 策略 3 箱体(信号型)—— 直接克隆 `backtest_s02.py` 骨架
- 信号日 = `screen_box` 判 SELECT 的 t;进场 t+1 收盘。
- **两套离场并报**:①固定持有期前瞻收益(5/10/20 日,纯信号质量);②`position_backtest.simulate_position` 状态机(沿用趋势深跌反包离场,**与 S01/S02 同口径便于横比**)。
- 指标:胜率 / 中位·平均收益/笔 / 盈亏比 / 相对沪深300 Alpha / 即死率(硬止损占比)/ 平均持有 / 样本数 + 统计显著性(t/p)。

### 2.2 策略 4 动量 A 腿(排序型)
- 每个 rebalance 日(每 5 / 10 日)按动量分排序 → **TopK 等权组合**持有到下次调仓;算组合日收益 → 年化 / Alpha / 夏普 / 最大回撤 / 换手。
- 并报 **IC**:每日全A 动量分 vs 前瞻 N 日收益的 Spearman 相关(时序均值 + ICIR),看动量分**有没有预测力**。
- 并报**分层**:按动量分十分位,每层前瞻 N 日均收益,看是否单调(动量有效则 top>bottom)。

### 2.3 策略 0 合议(排序型)
- **IC 为主**:每日全A 合议综合分 vs 前瞻 5/10/20 日收益的 Spearman 相关(时序均值 + ICIR + t 检验)。
- **分层多空 spread**:合议分十分位,Top decile − Bottom decile 的前瞻收益差(合议有效则显著 >0)。
- **TopN 组合**:每日 Top20 等权前瞻收益 vs 沪深300 Alpha(贴近实际用法)。
- 边界:合议里资金流/情绪/多因子/事件维度在全A最小记录下**弃权**(无历史快照),故测的是"**技术面为主的合议**",与 web 全维度合议**不等价**,须标注。

---

## 3. ⭐ 预注册预期(结果出来前写死)

> 依据:① 本轮实证(放量突破不提收益、上涨概率/买卖倾向在全A无 edge、区间肥尾);② A股已知特性(短期反转>动量、假突破多);③ S01/S02 前车(信号型机械基线普遍贴地/微负)。

| 策略 | 我的预测 | 判"有效"的判据 | 判"无效"的判据 | 若无效·最可能原因 | 预设调参方向 |
|---|---|---|---|---|---|
| **3 箱体** | **弱/负超额**(放量突破在A股假突破多,类 S01 即死) | Alpha 显著>0 且即死率明显低于 S01(<50%) | Alpha≈0 或负、即死率高、p 不显著 | 箱体突破=接下跌中继/假突破;放量不提收益(本轮已证) | 提突破放量倍数、加"回踩不破再进"确认、只在趋势 regime 用 |
| **4 动量A腿** | **短周期(5-10日)弱/负,20日或趋势市才可能正**(A股短期反转) | IC 显著>0(如 5日 IC>0.03 且 ICIR>0.3)、分层单调、TopK Alpha>0 | IC≈0/负、分层不单调 | A股短期动量弱、反转占优;R²闸门没滤掉伪趋势 | 拉长动量回看/持有期、强化 R²质量门、叠反转过滤 |
| **0 合议** | **IC 小(\|IC\|<0.05)、TopN Alpha 微弱**(买卖倾向本轮已显示无 edge;合议维度更多但同源技术信号) | IC 显著>0、Top−Bottom spread 显著>0 | IC≈0、spread 不显著 | 纯技术面合议信息量弱;弃权维度(资金流/情绪/基本面)恰是 alpha 来源却缺席 | 补历史快照让资金流/情绪/多因子维度上场;调专家权重;换排序目标 |

**总预期(诚实)**:三个大概率**都跑不出强正 alpha**——与本轮"没有任何策略/预测项有统计显著正超额"一致。这不是唱衰,是让"若结果确实平/负"时**不意外、不硬凑**;若某个**意外地正**,则要先查是不是样本偏差/未来函数泄漏(反向自证)。

**成功的真正定义**:不是"逼出正 alpha",而是**给每个策略一个带样本量、带显著性的诚实数字 + 一个数据支持的下一步**(要么可调、要么该弃、要么缺数据待补)。

---

## 4. 实施拆解(先框架后填充,能并行则并行)

1. **箱体回测**(最干净,现成模板):克隆 `backtest_s02.py` → `backtest_box.py`,换信号源为 `screen_box`,离场沿用。**先做,立即能出数。**
2. **排序型回测器**(动量+合议共用):新建 `backtest_rank.py`——输入"每日全A 打分函数",产出 IC / 分层 / TopK 组合绩效。动量分=`weighted_log_momentum`;合议分=`council` 综合分。两策略共用一套,只换打分函数。
3. **单测**(约法6):无未来函数(构造未来不改信号/打分)、IC 口径、分层单调性构造校验。
4. **文档回填**:各出结论表 + 对照本预注册"预测 vs 实际",更新本文件 §5。

**工作量估(agent 视角,约法11)**:
- 箱体回测:token ~30-50K,agent ~1h(克隆+跑+单测)。
- 排序型回测器:token ~60-90K,agent ~2-3h(新 harness + IC/分层 + 单测 + 两策略各跑)。
- 可并行:箱体(信号型)与排序型 harness **互不依赖、不同文件** → 可并行起 subagent。合议与动量共用排序器,串行(先器后两次跑)。

---

## 5. 结果(2026-08-13 回填)

> 全程无未来函数(切片重放 + 真实日期对齐);数据落 `data/analysis/backtest/`。harness:`backtest_box.py`(信号型)、`backtest_rank.py`(排序型)。

### 5.1 数字表

| 策略 | 样本 | 关键数字 | 判定 |
|---|---|---|---|
| **3 箱体** | 全A 5208 → 有效样本、**N=1415 笔** | 胜率 31.2% · 中位 −3.0%(=硬止损)· 均收益 +0.74% · 盈亏比 3.03 · **相对沪深300 Alpha +0.32%** · 即死率(硬止损)61% · 平均持有 4.2 日 | 🟡 弱正,≈S02 档次;Alpha 经济上极小、大概率不显著 |
| **4 动量A腿** | 随机 499 只 · 4.5万观测 · 273 日 | **IC:5日 −0.048(t=−2.91)/ 10日 −0.067(t=−3.87)/ 20日 −0.069(t=−4.04),显著为负** · TopK(实际选股口径)超额 −0.03~−0.13% · Top−Bottom 全负 | 🔴 **显著负**,A股短期反转,选强动量票跑输均值 |
| **0 合议(技术面)** | 随机 299 只 · 1.6万观测 · IC 有效 98 日 | IC:5日 −0.011(t=−0.55)/ 10日 −0.025(t=−1.13)/ 20日 −0.031(t=−1.31),**均不显著** · TopK 超额 −0.05~−0.58% | 🟡 无 edge、不显著;近 0 微负 |

### 5.2 预测 vs 实际(预注册对照)
- **箱体**:预测"弱/负"→ 实际**弱正 +0.32%**(我略偏悲观),但即死率 61% 仍高、方向对。**部分命中**。
- **动量A腿**:预测"短周期弱/负(反转市)"→ 实际**显著为负**。**完全命中**,且反转特性有统计显著性。
- **合议**:预测"IC 小(<0.05)、微弱"→ 实际 |IC| 0.01~0.03、不显著、TopK 负。**命中**。
- **总预期**(三者都跑不出强正 alpha)→ **成立**。无一意外为正,无需反向自证泄漏。

### 5.3 数据支持的下一步(各自)
- **策略3 箱体**:弱正但靠不住(即死 61%)。按预注册调参方向试:①突破放量倍数↑;②"回踩不破再进"确认(降假突破,呼应本轮 τ 结论);③只在趋势 regime 用。调不出显著正超额则**降级为"辅助形态标注",不单独当买点**。
- **策略4 动量A腿**:**显著负是硬结论**——纯动量在 A 股短期是负 edge。选项:①**弃用**纯动量选股;②**反做**(低动量/反转因子)——D9-D0 虽微正但 TopK 口径受损,反转在中段最强;③只当"趋势确认位"辅助,不做独立选股专家。**不建议现状上线**。
- **策略0 合议**:技术面合议信息量≈0。关键局限:**资金流/情绪/基本面/多因子维度在全A弃权**(无历史快照),而这些恰是 alpha 来源。→ 要让合议有意义,得**先补历史快照**让这些维度上场;纯技术面版不够。当前更适合当"多信号一致性/风控过滤",非选股 alpha 来源。

### 5.4 方法学局限(诚实)
- 箱体用 S01 离场状态机(机械基线),非最终买法;Alpha 未算 t/p(量级极小,≈S02 已知不显著)。
- 排序型样本为随机抽样(动量 499 / 合议 299),窗口约 1.5 年、偏多;合议 IC 有效日仅 98(min_cross=10 过滤),统计力中等。
- 合议 = **技术面子集**(全维度弃权),不等价 web 全维度合议。
- 复现:`python -m tools.backtest.backtest_box --no-view`;`python -m tools.backtest.backtest_rank --score momentum|council --sample N --step K`。

---

## 6. 反转因子探索(2026-08-13,追"动量显著为负"这条唯一正向线索)

> 动量 IC 显著为负 → 反过来"低动量/反转"可能有 edge。按"数据说话·多可能性分测":rev5/rev10(纯 N 日反转)、rev_mom(反做加权对数动量)、+ rev5 流动性前50% 可交易性检验。命门判据(预注册):**多空 Top−Bottom 净成本后显著正才算真 edge**。harness:`backtest_rank.py`(加 rev* 打分 + 成交额流动性列/过滤 + 多空净成本年化)。

| 变体 | IC(t) | 多空净成本年化% | 结论 |
|---|---|---|---|
| rev5 | 5日+0.012(0.72)/20日+0.022(1.45) 不显著 | **−14 / −11 / −10.5** | 买极端跌票反跑输 |
| rev10 | 10日+0.032(2.0)/20日+0.039(2.57) 显著 | −27 / −14 / −7.6 | IC正但多空净负 |
| rev_mom(反做动量) | +0.048/+0.067/+0.069(t 2.9~4.0)显著 | +0.5 / +1.7 / +1.6 ≈**零** | IC显著但净成本吃光 |
| rev5·流动性前50% | ≈0/负 | −11 / −3 / −11 | **edge 在垃圾小盘,可交易票里消失** |

**结论(诚实)**:
1. **反转统计上真实**(rev_mom IC 显著正,负动量的镜像)——A股短期确有反转倾向。
2. **不可交易**:分层非单调(非干净"买跌")、多空净成本后≈0(rev_mom)或负(rev5/10);
3. **edge 集中在低流动垃圾票**:保留流动性前50%后 IC 归零/转负。
→ 命中预注册命门"看着能赚、交易就没了"。**按判据反转不成立,不建议做成策略。**

**未再深挖(避免数据挖矿)**:观察到"关系非单调"——极端最强动量档(D9)在动量测里 20日 +2.07% vs 均值 +1.02%,但最极端 Top20 名反而跑输(吹泡沫顶)。提示"强而不极端的动量"可能是另一条线,留作**下一轮独立假设**(需先预注册再测,不在本轮顺手挖)。

## 7. 本轮总结论(策略侧)

**追完唯一正向线索后,结论更硬:目前没有任何可部署的正 edge。** 三个选股策略(箱体弱正不显著/动量显著负/合议持平)+ 反转探索(账面异象、不可交易)全部未跨过"净成本后显著正"的门槛。这不是失败——是用数据挡住了几个"看着专业但会亏钱"的策略上线。真要找 alpha,方向是**补数据维度**(资金流/情绪/基本面历史快照让合议有料)或**换资产/频率**,而非在纯量价日频里继续挖。
