# 融合基线设计:合议0 × 策略11 洗牌重建(融合北极星·第一步)

> 2026-08-30 · 统筹 · 用户已拍板(按推荐默认)。这是"技术面×消息面融合"北极星的第一步:先把技术面好信号复合成一条**小而精(5–10股)**的融合基线,消息面叠加留 WI-6。分支 `feat/council11-fusion-baseline`。

## 一、融合原料(取精华·去糟粕,均有数据支撑)
| 信号 | 来源 | 数据依据 | 用法/权重 |
|---|---|---|---|
| 超买超卖 | 合议好专家 | rank-IC +0.040(最稳) | 核心排序,正权重 |
| 拐点 | 合议好专家 | Top10超额+0.105(p=0.028) | 过滤/次级,正权重 |
| 技术趋势(反用) | 合议差专家 | rank-IC **−0.046 显著反向** | **负权重反转因子候选**,回测达标才引入 |
| 策略11 状态条件指标 | 策略11 | 框架有用(原始排序非alpha) | 结构化技术状态输入(用指标不用其离散排序) |
| ~~动量~~ | — | edge只在1日,并入会周期错配 | **不并入**,动量保持独立策略 |

## 二、已拍板口径(用户确认)
1. **合成权重**:先等权 zscore 复合(基线),再对比"按历史IC加权";两版都回测报。
2. **Top-N**:主榜 **Top10** + 精选 **Top5** 两档并列。
3. **动量不并入**融合基线(独立保留)。
4. **技术趋势反用**:纳入回测,**达标(正增量)才给负权重**,否则不引入。
5. **策略11 吸收后下线**:其状态条件指标作输入,原独立"指标条件化状态排序"策略从生产链路下线(config 谨慎,被复用则保留)。

## 三、执行两阶段(不达标不硬上)
- **Phase A 建融合分 + 回测验证**:复用 eval_v3 口径(rank-IC/Top-N/按日聚类/T+1收益+close[T]命中/gross-net扣成本),与现状 council 等权基线**对比增量**。**达标判据(预注册)**:融合分 rank-IC 显著正 ∧ Top-N 扣成本超额显著正 ∧ 明显优于 council 等权。达标→Phase B;不达标→停下如实报"未达标",不硬接生产。
- **Phase B(仅达标)**:新策略 screener+score接入+strategy.json登记(达标授编号,暂名"技术合议融合")+前端+单测;策略11 下线;修 PR#19 隐患1(齐全度稀释:核心必备 vs 可选增强分层)；消息面预留接口(不实现)。

## 四、诚实边界(预期管理)
合议0 拆分重建的 edge 本就**小**(删技术趋势后 rank-IC+0.041,但 Top5 扣成本仅薄有余量、Top10≈打平)。融合基线**很可能只是"止血+小幅正偏"**,不是高收益。若 Phase A 显示融合分也过不了达标线,如实停下、不硬上——这正是两阶段+预注册判据的目的。

## 五、状态
- 2026-08-30:设计获用户认可,Phase A 线已起。等 Phase A 回测结论定夺是否进 Phase B。
