# 2026-08-30 大版本迭代:删显著负 + 合议0×策略11洗牌重建 + 存活策略精修 + 融合北极星(统筹计划)

> 统筹窗口维护。承评测方法 v3 全口径裁定(见 `docs/策略成绩报告.md` §六 + 开发日志 2026-08-29),用户拍板本轮大版本迭代。工作模式=子 agent + worktree 内容窗口;统筹只派发/核范围/独立临时 worktree `--no-ff` 集成/账本。**不需要新用户窗口。**
> 根本方向:**技术面 × 消息面融合**;最终形态=每次选股**小而精 5–10 股**,先把技术面好东西做上、再叠消息面(策略11 的断点/评价参数用法)。

## 一、工作项(6 件)
| # | 工作项 | 内容 | 归属阶段 |
|---|---|---|---|
| WI-1 | 删两个显著负策略 | S01 趋势深跌反包 + 箱体3:生产链路摘除(strategy.json/run/前端/记分卡),代码存档保留 | 阶段1 |
| WI-2 | PR#19 修复 | chip 的 as_of point-in-time(融合要用)+ consensus/主力 新鲜度门控增量拉。**齐全度稀释归 WI-3** | 阶段1 |
| WI-3 | 合议0×策略11 洗牌重建 | 抽好专家(超买超卖+拐点)、删技术趋势、与策略11 混成新融合基线;顺带修 PR#19 齐全度稀释。**融合北极星第一步** | 阶段2 |
| WI-4 | 存活策略精修 | ①Top-N化(动量/最大范围/量价按分排序取 Top5/10)②预测周期(1日策略只出隔日推荐、不出5日)③薄样本改名"待观测策略"④独立页选股统一进一页排版(像 SEPA) | 阶段3 |
| WI-5 | 文档与日志 | 工作日志公示 + 策略文档整修 | 贯穿 |
| WI-6 | 融合完全体(北极星) | 技术面精华抽取+配权 → 叠消息面 → 小而精 5–10 股。WI-3 是第一步,完全体单独立项 | 后续 |

## 二、解耦与并行(为什么这么排)
**共享地基 = `config/strategy.json` + `tools/analysis/factor·experts` + `web 模板`**,删/重建/Top-N/待观测/页面全碰它们,多线抢改必撞 → 分三阶段:

- **阶段1(并行 2 线)**:α=WI-1 删(strategy.json/screener/web-移除)‖ β=WI-2 PR#19(collectors/serialize/run,与 α 文件基本不相交)。
- **阶段2(串行 1 线·大)**:WI-3 洗牌重建 + 齐全度修复,基于阶段1 后干净 roster,独占 council/factor,不并行。**开工前统筹先出「重建+融合基线设计」给用户审。**
- **阶段3(并行 2 线)**:4a=Top-N+预测周期+待观测(config/screener/prediction)‖ 4b=页面排版统一(web),文件面 config vs web 基本不相交。基于阶段2 后最终 roster。
- **贯穿**:WI-5 由统筹每次集成顺手更 + 收尾。
- **后续**:WI-6 融合完全体,阶段2 后单独出融合设计文档。

## 三、子 agent 安排(全程子 agent,无新窗口)
| 阶段 | 子 agent 线 | 分支 |
|---|---|---|
| 1 | α 删 S01/箱体3 | feat/remove-s01-box3 |
| 1 | β PR#19 chip as_of + 门控 | fix/pr19-chip-asof-fetch |
| 2 | WI-3 洗牌重建 | (阶段1 后建) |
| 3 | 4a 精修 / 4b 页面 | (阶段2 后建) |

每阶段回来:统筹核范围 → 独立临时 worktree `--no-ff` 合 → 不带 `--force` 清理 → 更账本。

## 四、状态(时间倒序)
- 2026-08-30:计划获用户认可。阶段1 α/β 两线已并行拉起。阶段2/3 待前序合入后推进。
