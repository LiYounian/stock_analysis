# 情绪打分失败不再静默兜底成"中性 0.0" —— A+B+C 重构设计

> 日期：2026-09-04　　分支：`feat/情绪打分失败三态`
> 触发：09-04 复盘发现 300802 矩子科技"买入·首选"是在消息面盲区下给出的。深挖确认：**采集层正常**（24 新闻/50 股吧/180 百度均在 18:36 闭环前落盘），是**情绪打分 LLM 层大面积失败**（09-03 参与选股的 564 只里 43% "全部新闻打分失败"、条目级 66% 失败；舆情层同日 55% `Connection error`），而失败被**静默兜底成 `强度0/中性/评论空` → 净情绪 0.0**，与"真中性"不可区分，`provenance` 也不反映。
> ⚠️ 非投资建议，测试环境研究模拟。

## 一、问题定性（一句话）

**"打分失败"和"真的中性"目前共用同一个 0.0 表示，缺失被冒充成正常值。** 这与近期 fundflow"过期值当当日值 + provenance=true"（commit `07f6469` 想解决的那类问题）是同一个病。后果：消息面完全是盲区的票照样走完打分、拿到全批最强表态，该有的置信度折价没打。

## 二、核心契约：三态（先定死，A/B/C 都建在它上面）

任何"会失败"的打分层，必须区分三态，**绝不允许把失败/缺失塌缩成 0.0/中性**：

| 态 | 语义 | 净情绪取值 | 下游处理 |
|---|---|---|---|
| **ok** | 打分成功，值可信（**可以是真·中性 0.0**，但那是真的中性） | 真实值 | 正常参与 |
| **unknown（未知）** | 打分失败/降级（LLM 超时、Connection error、并发打爆） | **null，不得写 0.0** | 触发置信度折价 + 表态封顶 |
| **missing（无数据）** | 本就没有输入（当天没采到任何新闻） | null | 该层退出加权、其余层重归一 |

### 落点（三个粒度）

1. **条目级**（`news_ai/<code>.json` 每条的 `ai`）
   - 新增显式 `ai.scored: true/false`（C 前向写入）。
   - 历史数据无该字段时，回退启发式：`评论` 与 `原因` **都空** ⇒ 视为 `scored:false`（失败兜底形态）。

2. **层级**（`sentiment.三层.{新闻,舆情,政策}`）
   - 新增 `status ∈ {ok, partial, unknown, missing}` 与 `失败数`；`样本数` 收窄为**成功打分**条数。
   - 新闻层：全失败 ⇒ `unknown`；部分失败 ⇒ `partial`（只用 ok 条目加权）；无新闻 ⇒ `missing`；全成功 ⇒ `ok`。
   - 舆情层：`degraded` 非空（如 `Connection error.`）⇒ `unknown`。

3. **顶层**（`sentiment`）
   - 新增 `质量 ∈ {ok, partial, unknown}` 与 `覆盖率`（成功层加权占比）。
   - `质量==unknown`（有效层覆盖不足阈值，默认 <0.3）⇒ `净情绪分 = null`，**不得为 0.0**。
   - `provenance.sentiment` 从"隐式布尔"改为三态 `{ok|partial|unknown|missing}`。

## 三、分工与文件边界（据 Explore 接缝图修正为串行单线）

> 依赖：**C 产出失败标记 → B 消费它做聚合与降级 → 表态降级 → A 暴露/标注**。契约（第二节）是共同接口。
> ⚠️ **接缝图结论：B(点2 `aggregate_sentiment`/`_weighted_net`/`analyze_stock`) 与 C(点1 `_one`/`ugc_sentiment`) 都在 `tools/analysis/event.py`**，且整条是 1→2→3→4 线性数据依赖 —— **不并行，一条线串行做**（异文件并行只会撞 `event.py` 且把契约拆散）。
> ⚠️ **选股 md 无代码渲染器**：`docs/每日分析/选股/<日>.md` 由选股 agent 手写（表态是分析师正文写的，非模板产出）。故 A 不改渲染代码，改为"让 record 带质量标记(B 产出) + 给选股 workflow 加'情绪质量必标'说明"。

### 关键接缝（`文件:行号`，来自 Explore）
| 点 | 位置 | 说明 |
|---|---|---|
| 1 失败兜底 | `news_ai.py:25 _degraded_ai`/`:42 _to_ai`；`event.py:212 _one`(:216 except→error)、`:437 ugc_sentiment`(:441 "Connection error."返0.0) | 逐条/舆情失败在此塌缩成中性0空 |
| 1 可靠性 | `event.py:160 _pmap`(ThreadPoolExecutor，**无重试/超时**)；重试超时应加在 `tools/llm/client.py` 的 `client.extract` | 09-03 66% 失败疑并发打爆 |
| 2 聚合 | `event.py:292 aggregate_sentiment`(:296 失败条目 continue **剔除**)、`:451 _weighted_net`(缺层重归一) | **舆情降级返 净情绪0.0+样本>0 会当真0混入加权** ← 活体入口 |
| 3 provenance | `serialize.py:478-524`(布尔字典，无情绪打分成功率字段)；契约 `tools/contracts/record.py` | 加情绪质量三态字段 |
| 4 表态 | `council.py:185 _bias_experts`(:229 `net>0 且 样本数>0` 才投情绪票)；镜像 `predict.py:200-210`；权重真源 `tools/config/strategy.py` | 区分"情绪弃权(无数据)"vs"打分失败(不可信)" |

### C · 打分层可靠性（产出失败标记）
- **改**：情绪 LLM 调用封装（逐条新闻 + 舆情）。
  - 加**重试**（指数退避）+ **超时** + **并发上限**（09-03 66% 失败大概率是并发打爆/限流）。
  - 最终失败**显式写 `ai.scored:false`**（及层级 `degraded` 原因），**不再静默回落到 `中性/0/空`**。
- **测试**：mock LLM 抛连接错误 ⇒ 断言 `scored:false` 且不产生 0.0 净情绪；mock 部分成功 ⇒ partial。

### B · 聚合语义 + provenance 三态 + 表态降级（消费失败标记）
- **改①（聚合）**：净情绪计算只吃 ok 条目/ok 层；`质量` 与 `覆盖率` 写入；unknown ⇒ 净情绪 null。
- **改②（provenance）**：`provenance.sentiment` 写三态。
- **改③（表态/综合分）**：`情绪质量==unknown` 时 —— (a) 情绪维度退出综合分并对其余维度重归一；(b) **打置信度折价**；(c) **表态封顶：不得给最强档（如"买入·首选"）**，强制降一级。
- **测试**：构造"情绪 unknown"的票 ⇒ 断言拿不到最强表态、综合分不被 0.0 情绪拉偏；构造"真中性"的票 ⇒ 断言仍可正常评级（锁住 ok 的 0.0 与 unknown 的 null 语义不同）。

### A · 情绪质量可见（无代码渲染器 → 两件事）
- **(i) 数据侧**：record 的 `sentiment.质量`/`覆盖率` 由 B 产出（已含在 B 里），选股 agent 写 md 时可直接读。
- **(ii) 流程侧**：给选股 workflow 说明加一条硬约束——**每票必标情绪质量**（`情绪: N/M 成功` 或 `情绪: 未知(打分失败)`），情绪 unknown 的票**不得给"买入·首选"**（与 B 的表态封顶呼应，形成"代码兜底 + 人工纪律"双保险）。此条由统筹改选股 workflow 文档，不进本实现线。

### 执行方式（串行单线）
- 一条实现线在本 worktree 上按 **C(点1) → B(点2+3+4)** 顺序推进，分步 commit。
- `event.py` 内 C 与 B 按函数严格切：C 只改 `_one`/`ugc_sentiment`/`_degraded_ai` 的 return 形态；B 只改 `aggregate_sentiment`/`_weighted_net`/`analyze_stock` 的消费与组装。先 C 后 B，同文件不同函数、串行无冲突。

## 四、预期效果（改完应看到的现象）

1. 300802 这类"情绪 unknown"的票，在选股环节**拿不到"买入·首选"**（被降级），且 md 上明确标"情绪未知/打分失败"。
2. `sentiment.净情绪分` 不再出现"失败导致的 0.0"——失败一律 null + 质量=unknown。
3. 09-03 那种 66% 条目失败的日子，C 的重试/并发退避应把失败率显著压低；仍失败的部分被如实标记、不污染净情绪。
4. 回归测试锁住：**ok 的真·中性(0.0) 与 unknown(null) 是两种东西**，未来重写不得再把它们合并。

## 五、开发顺序（分步 commit，守则9）

1. 【本文档】设计评审留痕。
2. C：LLM 调用可靠性 + 显式失败标记（+测试）→ commit。
3. B：聚合三态 + provenance + 表态降级（+测试）→ commit。
4. A：选股 md 标注（+测试）→ commit。
5. 全量测试绿 → 合 main。

## 六、开放项（实现时据 Explore 接缝图定）

- 覆盖率阈值默认 0.3，是否需按层加权后再判——实现时定，写进测试。
- 历史数据（09-01~09-03）是否需要一次性回填 `质量` 字段？倾向**不回填**（历史 record 保持原样，只让新管线前向正确；回填另开一次性脚本，类比 `ops/fix_turnover_unit.py`），避免动 data/。
- "表态封顶降一级"的具体档位映射，取决于 Explore 查到的表态枚举。
