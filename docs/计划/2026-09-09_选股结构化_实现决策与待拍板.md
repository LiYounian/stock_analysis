# 选股产物结构化（方案B · P1 实现）· 已定口径 + 待用户拍板

> ⚠️ 非投资建议，研究模拟。配套：`docs/计划/2026-09-09_选股产物结构化与程序化_方案.md`（完整设计）
> 、`docs/日志/开发日志.md`（本轮实现要点）。
> 本轮范围：**只落 P1 Tool + 测试，未接生产**（未改 SKILL / 定时任务）。

---

## 一、实现窗按方案推荐值已定的口径（无争议，直接落）

对应方案 §7 的 7 个拍板点，实现窗按方案推荐默认取值，未卡住：

| # | 点 | 本轮采用（方案推荐） |
|---|---|---|
| 1 | 日期命名策略 | **文件名仍按执行日、靠 doc.meta 顶部声明归属日消歧，不重命名历史文件**。doc.meta 含 `pick_date`(D)/`predict_for`(D+1)/`as_of`(防未来锚点)。 |
| 2 | view 命名 | **中文 `每日选股`**（与现有 `选股分析报告`/`selection_analysis` 一致；随 `collect_date` 自动成 `__view__:每日选股`）。 |
| 4 | Tool vs MCP | **轻量 Python CLI**（`python -m tools.analysis.write_picks`），与 upload/task_guard 调用范式一致；MCP 留后续多协作者接入再升级（`build_picks_json` 可直接包一层）。 |
| 5 | 情绪质量闸门代码化 | **已代码化为硬断言**：`sentiment_quality ∈ {unknown,missing}` 时 `stance` 不得 `买入`、`stance_qualifier` 不得含 `首选`，校验不过即拒绝落盘（对齐机制 §4 规则7）。 |

**免改传输层**：`每日选股.json`（非 6 位文件名）→ `import_to_db.collect_date` 自动归 `views["每日选股"]`
→ `upload.py` 自动分片上传，**upload.py / import_to_db.py 零改动**（已用 tmp 目录跑通集成验证）。

---

## 二、待用户拍板（本轮未擅自决定，标注待决）

### 待决 1（方案 §7.3）新旧视图取舍与远端读取优先级
`每日选股`（新权威结构化源）与 `selection_analysis`（旧 md 正则兜底）**短期并存**。
- 建议：远端读取优先级 **`每日选股` 新 → `selection_analysis` 旧兜底**；存量 42 篇老 md 无 JSON，
  仍走旧 `selection_summary`。
- **待拍板**：旧 `selection_summary` 是**长期保留**（历史 md 永久兜底）还是**限期迁移后废弃**？
  （远端读取优先级实现属远端窗，本仓只约定契约。）

### 待决 2（方案 §7.6）实施排期：本轮之后何时接生产
本轮只落 P1 Tool + 测试，**未改 `daily-stock-selection/SKILL.md`、未接定时任务**。
- **待拍板**：P2（改 SKILL 增一步调用 write_picks + 跑一个交易日实测）是**下一轮就接**，
  还是**先观察 Tool 稳定性再接**？（建议：先观察一个交易日 dry-run 产物形态再接生产，避免带错落盘。）

### 待决 3（方案 §7.7）远端 P4 渲染排期
`每日选股` view 的远端 web 渲染页/列（带名称/行业/表态/方向）属**本仓外**改动。
- **待拍板**：本轮是否就协调远端窗做渲染，还是先在本地/DB 存着、远端后跟？

### 待决 4（方案 §3.3 规则1 · 建议项）复盘/日内标题补归属日声明
方案建议把复盘标题统一成 `# 复盘 · D（复盘 D-1 选票 → 对 D 实际）`、日内 `# 午盘研判 · D（跟踪 D-1 选票）`。
- 本轮**未改任何历史 md 正文**（避免破坏存量）。
- **待拍板**：是否要在 SKILL 模板层加"标题必带归属日声明"的硬要求（仅影响未来产物，不改历史）？

### 待决 5（方案 §3.4 · 可选增强）`选股索引.json` 总账
新增 `data/analysis/选股索引.json`（一行一天：status/picks_count/predict_for/reviewed）给远端总账。
- 本轮**未实现**（属 P3）。**待拍板**：是否需要，与 P3 复盘 `review` 回填一起做。

---

## 三、本轮交付物

- `tools/analysis/write_picks.py`：canonical 组装 + 客观回填 + 校验 + 原子落盘 + md 渲染 + CLI。
- `tests/test_write_picks.py`：19 例全绿（schema 校验 / 回填 / 策略 join / 防未来 / skipped / PICKS 一致性 / md / 落盘）。
- `picks_schema.py`：沿用 P0 骨架为枚举与纯结构校验单一真源（未改）。
- 开发日志条目。
