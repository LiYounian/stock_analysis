# 推进总盘（多线并行·统筹追踪）

> 用途：统筹窗口维护的**全局工作盘**——记录多窗口/多 sub agent 并行推进的所有工作线、状态、依赖、决策台账、在编窗口。转交他人/新模型时读此文档即可对接。
> 维护者：统筹窗口（唯一 main 集成者）。每有完工合并/决策/派工变化即更新。
> 最近更新：2026-09-10 凌晨（REVS 收官）｜ 主仓基线 `6b93251`
> ⚠️ 非投资建议，研究模拟。

## 状态图例
✅ 已完成合入 main ｜ ⏳ 在跑 ｜ ⏸ 待前置 ｜ 🅿 待用户拍板/排期

---

## 1. 本轮已落地（合入 origin/main）

### 数据获取层整顿（数据源窗）
| 线 | sha | 要点 |
|---|---|---|
| P1 当日spot换腾讯批量 | `31e8926` | gtimg批量+量纲手→股/万元→元归一+量额一致性硬断言防100×/1e4×、WORKERS 1→8/去sleep0.5；治东财超时(全A~20s) |
| V回填(估值历史) | `bff4dea` | `_fetch_baidu`存整条历史序列(新store kind=valuation:date×PE_TTM/PB/总市值)；全市场回填5557只/均804行/零失败 |
| P1-3 资金流三级源 | `def0309` | 东财→腾讯proxy→新浪三级fallback(纯requests不吃TLS墙)+恒等式硬断言+缺列NaN非0+降级标注 |
| 接入设计+2决策 | `d287eb6` `1297d83` | 选源=腾讯proxy+新浪、宏观R=方案A(数据线建macro.py) |
| P0诊断设计(仅设计) | `38f4ac4` | 主档as_of/复权语义(风险F/G/D+方案P0-1/2/3)，实施等P1稳一天 |

### 选股/策略
| 线 | sha | 要点 |
|---|---|---|
| screen_strong 阶段二 | `188a039` | 最强选股筹码切本地chip(STRONG_CHIP_SOURCE默认local/tushare一键回退)，去唯一Tushare依赖；20:00 job保留对照 |
| 收盘富集并行化 | `0410aad`一带 | screenall富集LLM接workers(消息面富集.并发数=4)，治"选股5小时"串行瓶颈 |
| 方案B P1(选股结构化) | `72d6edc` | `write_picks.py` Tool + canonical每日选股JSON(名称/行业/价代码回填)+校验；只落Tool未接生产 |

### REVS 四因子（REVS设计窗）——已结论：证伪合成、捞出「估值位」入合议
| 阶段 | sha | 结论 |
|---|---|---|
| 阶段1(E/V/S六件套) | `485a50c` | E+S稳健正α但相对单维增量seed不稳、V未测；留痕不上线 |
| 阶段1b(全量V) | `b2de9c6` | **V估值=真因子**(IC三seed显著正)；**四因子合成证伪**(打不过E单维) |
| V入合议评估 | `b262a96` | V⊥E质量(Spearman−0.097)纯增量、合议已有价值槽(PE/PB)只缺市值分位 |
| V入合议实施+两级A/B | `fed5f7c` `71b8c1e` | 加市值分位:因子级net+5→+15~20%、全合议级IC升/net−5%→~0+4%(达标但被稀释、小市值敞口caveat) |
| **估值位坐实(翻开关入生产)** | `6b93251` | 「价值市值分位启用」False→True 入生产合议(A等权/命名估值位)+小市值敞口监控 `_estvalue_monitor`+config可回退；**REVS全线收官** |

### web 展示（/selection-analysis）——选股↔复盘对应+名称+α 全修好，远端已验
| 线 | sha | 要点 |
|---|---|---|
| 选股↔复盘对应+名称 | `d4d5172` | 复盘表改"对昨日D-1实际选出票"(非自选池)+名称回退+两列+跳过占位 |
| 复盘α多级回退 | `f460b1e` | `_selection_pct_at`:record→主档K线→复盘md收盘表→None |
| α复盘表补(09-09) | `8d26e3c` | 补"逐票收盘记分表"→远端8只α填满(601061+5.38pp/000019+10.48pp…) |
| lint修复 | `282b6d0` | 依赖方向lint放行equal_weight_index视图loader(修d4d5172引入的既有失败) |

**forward**：eod复盘 SKILL 已加"逐票收盘记分表"要求(今晚09-10复盘起自动产)，web一劳永逸解析。

---

## 2. 在跑 ⏳
（本轮实施线均已落地合入 main，无在跑；下方待续项等用户排期启动）

## 3. 待续（等前置 / 待排期）
| 线 | 等什么 |
|---|---|
| 数据源 P0（主档as_of/复权语义） | P1稳一交易日后接（设计已在`38f4ac4`） |
| 数据源 P2/P3（retry普及/批量并发/归一收口） | 低优先，后续 |
| REVS 阶段2（R regime + macro.py 方案A） | 排期；宏观R归属已定=数据线产出 |
| 方案B 接生产 P2（改SKILL调write_picks+跑交易日dry-run） | 待排期 |
| screen_strong 阶段三（退役20:00 strong job） | 灰度1-2周后（改launchd前查job在不在跑） |

---

## 4. 决策台账（用户已拍板）
| 决策 | 结论 |
|---|---|
| REVS 值不值得做 | 值得(分阶段+守回测闸门) |
| REVS 四因子合成 | 证伪不上线；捞出V |
| V入合议 | 翻开关坐实、**A方案等权**(不自定义子权重)、V命名「**估值位**」 |
| screen_strong切本地 | 方案A(阈值95)/默认local灰度/20:00保留对照 |
| 数据源选源 | 资金流二源=腾讯proxy+新浪三级链 |
| 宏观R归属 | 方案A：数据线建macro.py |
| V回填重采 | greenlight后台并行(已完成) |
| α修复方向 | eod写复盘scorecard表→web解析(前向)+手动补09-09 |

## 5. 在编窗口表
| 窗口 | session_id | 负责线 | 状态 |
|---|---|---|---|
| 统筹窗口(本窗) | `local_c3f60e01-…` | main集成+跨窗沟通+本盘维护 | 在岗(心跳保活 `stock-coordinator-heartbeat` :08/23/38/53) |
| 数据源窗 | `local_e840c1e4-…` | 数据整顿(P1/V/P1-3✅;P0待) | 待命 |
| REVS设计窗 | `local_5cbba138-…` | REVS→估值位入合议(翻开关实施中) | 活跃 |

> 跨窗沟通：认 session_id 投递、正文带暗号(数据线=「数据整顿-0909」/REVS=「四因子对齐」)、完工回执双向闭环。

## 6. 统筹纪律
- main只统筹集成；子窗/subagent独立分支基于最新origin/main、不碰main、完工回执统筹核验合并(cherry=0才删本地分支、他窗分支保留)。
- 合并走独立临时worktree(`--detach origin/main`→`merge --no-ff`→push)，绝不主仓checkout main；清理必须gated在合并成功之后；push前脱敏；改launchd前查job在不在跑。
- 需用户拍板的：对应窗写决策文档(对比/优劣/推荐)，用户在该窗回(3a)；统筹汇总不替拍。
