# 推进总盘（多线并行·统筹追踪）

> 用途：统筹窗口维护的**全局工作盘**——记录本轮多窗口/多 sub agent 并行推进的所有工作线、状态、依赖、决策台账、在编窗口。转交他人/新模型时读此文档即可对接。
> 维护者：统筹窗口（唯一 main 集成者）。每有完工合并/决策/派工变化即更新。
> 最近更新：2026-09-09 夜 ｜ 主仓基线 `bff4dea`
> ⚠️ 非投资建议，研究模拟。

## 状态图例
✅ 已完成合入 main ｜ ⏳ 在跑 ｜ ⏸ 待前置 ｜ 🅿 待用户拍板/排期 ｜ 📄 决策文档待回

---

## 1. 本轮已落地（合入 origin/main）

| # | 工作线 | 合并 sha | 要点 |
|---|---|---|---|
| 1 | 数据源 P1：当日 spot 换腾讯批量 | `31e8926` | `gtimg_quote.fetch_quotes` 批量、量纲手→股/万元→元归一 + 量额一致性硬断言防100×/1e4×、FETCH_WORKERS 1→8/去sleep0.5，akshare 降兜底；治本机东财 spot 超时（全A ~20s） |
| 2 | screen_strong 阶段二：切本地筹码 | `188a039` | S05 筹码取数 tushare cyq_perf → 本地 chip；`STRONG_CHIP_SOURCE` 默认 local、tushare 一键回退；去全项目唯一 Tushare 依赖；20:00 job 保留作对照未退役 |
| 3 | 方案B P1：选股产物结构化 | `72d6edc` | `write_picks.py` CLI Tool + canonical `每日选股.json`（名称/行业/价由代码从 record 回填，根治"远端只有代码没名称"）+ 校验（必填/防未来/情绪闸门）；自动归 view 分片上传；**只落 Tool 未接生产** |
| 4 | REVS 阶段1：E/V/S 四因子 | `485a50c` | 六件套留痕：因子后端 `revs.py` + 回测器 `backtest_revs.py` + 实盘管线 `screen_revs.py` + config块 + 24测试 + 诚实回测报告；**标"候选/前向观测·未授面板编号·未接每日编排"** |
| 5 | 数据源决策回填 | `1297d83` | 用户拍板：资金流二源=腾讯proxy hsfundtab主+新浪兜底（三级链）；宏观R归属=方案A（数据线建 macro.py） |
| 6 | 数据源 V历史：collector 修复 | `bff4dea` | `_fetch_baidu` 改存**整条历史序列**（新增 store `kind=valuation`：date×PE_TTM/PB/总市值），修"只留最新丢历史"缺陷；旧标量/PE分位口径不变；真百度探针验证完整历史 |

> 说明：本轮之前同会话已合入的（消息面回灌事件方向/时效修复、收盘富集并行化并发4、选股md锚点、财报哲学二、09-08选股补做、SKILL锚点、screen_strong阶段一校准等）见开发日志，不在此重列。

---

## 2. 当前在跑 ⏳

| 工作线 | 归属窗口 | 预计 | 完成后 |
|---|---|---|---|
| **V 全市场回填**（~5000票×3指标历史序列） | 数据源窗 | 并发 ~15-25min | → 解锁 REVS 阶段1b |
| **P1-3 腾讯proxy 可达性实测**（442只测是否吃TLS墙） | 数据源窗 | 小实测 | 可达→出设计→实现；不可达→选源返工 |

---

## 3. 待续（等前置 / 待拍板）

| 工作线 | 状态 | 等什么 |
|---|---|---|
| REVS 阶段1b（完整 E/V/S 回测） | ⏸ | V回填 done → 统筹拉 REVS 窗启动 |
| REVS 授面板编号 | 🅿 | 1b 回测过闸门 + 用户拍板 |
| REVS 阶段2（R regime + macro.py，方案A） | ⏸ | 阶段1达标 + 宏观R实施时机 |
| 数据源 P1-3 实现（资金流二源三级链） | ⏸ | 可达性实测结果 → 接入设计 → 实现 |
| 数据源 P0（主档 as_of/复权语义加固） | ⏸ | P1 稳一天后接 |
| 数据源 P2/P3（retry普及/批量并发/归一收口） | 🅿 | 低优先，后续排期 |
| 方案B 接生产 P2（改 SKILL 调 write_picks + 跑交易日 dry-run） | 🅿 | **待用户排期（当前唯一可即拍的）** |
| 方案B 其余待拍板点（新旧视图迁移/远端P4渲染/选股索引.json） | 🅿 | 详见 `2026-09-09_选股结构化_实现决策与待拍板.md` |
| screen_strong 阶段三（退役 20:00 strong job） | 🅿 | 灰度 1-2 周后（改 launchd 前先查 job 是否在跑） |

---

## 4. 关键依赖链

```
V回填(在跑) ──→ REVS阶段1b(完整E/V/S) ──→ 达标? ──是→ 授编号 ──→ REVS阶段2(R regime,需macro.py)
                                          └─否→ 诚实收场,E/V因子留作合议输入

P1-3可达性实测(在跑) ──可达→ 接入设计 ──→ 资金流二源实现
                     └─不可达→ 选源返工(新浪主?)

数据源P1(已合) ──稳定一天→ P0主档语义 ──→ P2/P3收口
方案B P1(已合) ──排期→ 接生产P2(改SKILL) ──→ 远端P4渲染
```

**REVS 立项核心论点**：E+S 阶段1回测显示本质是"重表达已知量价 alpha"（S=反转，与既有反转低换手同族），**REVS 的真正增量维是 V 估值**——所以 V回填→阶段1b 是 REVS 的 make-or-break 测试。

---

## 5. 在编窗口表（统筹维护）

| 窗口 | session_id | 负责线 | 状态 |
|---|---|---|---|
| 统筹窗口（本窗） | `local_c3f60e01-…` | main 集成 + 跨窗沟通 + 本盘维护 | 在岗 |
| 数据源窗 | `local_e840c1e4-…` | 数据获取层整顿（P1✅/V回填⏳/P1-3⏳/P0⏸/宏观R⏸） | 活跃 |
| REVS 设计窗 | `local_5cbba138-…` | REVS 四因子（阶段1✅留痕/阶段1b⏸待V/阶段2⏸） | 待命 |
| 桌面功能介绍窗 | `local_9e75faf7-…` | 通信规范周知 | 已完成·可归档 |
| 定时任务会话（选股/复盘/午盘/盘中） | 多 | 每日自动流水 | 无人值守·自动跑 |

> 跨窗沟通：认 session_id 投递（`send_message`）、正文带本轮暗号、完工回执双向闭环。暗号：数据线=「数据整顿-0909」、REVS=「四因子对齐」。

---

## 6. 决策台账（用户已拍板）

| 决策 | 结论 | 依据 |
|---|---|---|
| REVS 值不值得做 | 值得，分阶段+守回测闸门 | REVS 设计文档 §7 |
| screen_strong 切本地 | 方案A(阈值95不改)、默认local灰度、20:00保留对照 | 阶段一校准达标 |
| 资金流第二源选源 | 腾讯proxy hsfundtab主 + 新浪r0_net兜底（三级链） | 数据源窗决策文档 |
| 宏观R归属 | 方案A：数据线建 macro.py + kind=macro | 数据源窗决策文档 |
| V回填重采 | greenlight，后台并行跑、跑完汇报 | 用户直批 |

---

## 7. 统筹纪律（本轮遵循）
- main 只由统筹集成；各窗/sub agent 独立分支基于最新 origin/main、不碰 main、完工回执统筹核验合并（cherry=0 才删本地分支、远端/他窗分支保留）。
- 生产代码改动核验后合、纯留痕/文档从宽；改 launchd 前先查 job 是否在跑；push 前脱敏。
- 需用户拍板的：对应窗写决策文档（对比/优劣/推荐），用户在该窗回；统筹只汇总不替拍。
