# 测试健康 triage 报告(2026-09-16)

> 分支 `fix/test-health-triage`(基于 origin/main `18fbfa2`)。环境:`~/.conda/envs/stock_analysis`。
> ⚠️ 测试环境研究模拟,非投资建议。

## 一、权威红清单(origin/main 全量 `pytest tests/`)

在 origin/main 上全量跑一次(12min06s):**8 failed / 3163 passed / 7 skipped**。
7 个 skipped 是既有的 LLM/网络用例(`@pytest.mark.skipif(not lc.is_configured())`),
离线环境**已正确 skip**——**8 个红里没有一个是网络失败**(任务清单里"疑需网络"的三条
经实测都不是网络原因)。任务给的已知红清单部分**已 stale**:`test_selection_page`(3 条)、
`test_semi_growth_exemption`、`test_sentiment_judge::shadow_roundtrip`、
`test_exchange_single_source` 里除"防复发"外的用例,**在 origin/main 上现已全绿**,以本次
实测为准。

| # | 用例 | 分类 | 根因(一句话) |
|---|------|------|------|
| 1 | `test_exchange_single_source::test_防复发_不得新增自己判前缀的实现` | (c) 真回归 | 本轮新增 `factor_nextday.board_limit`、`selection_alpha/nextday_kernel.board_limit` 自判 30/68·300/688 前缀,闸门如实拦下 |
| 2 | `test_experience_doc_lint::test_entry31_...` | (c) 真回归(数据) | 经验库 v2026-09-14.md 的 #31 锁定条款被自动 EOD 提交覆盖删除 |
| 3 | `test_experience_doc_lint::test_entry10_...` | (c) 真回归(数据) | 同上,#10 ⑤capitulation/⑥弱市不硬凑 + 状态字段被删 |
| 4 | `test_experience_doc_lint::test_weak_market_no_forced_fill` | (c) 真回归(数据) | 同上,弱市"不硬凑/只有N只够格"条款随之丢失 |
| 5 | `test_news_recall::test_relevance_filter_passes_disable_thinking` | (b) 契约脆弱 | client 重构把"关思考"从 create() 现读 settings 改为实例属性 `_disable_thinking`;用例绕过 `__init__` 未补该属性 |
| 6 | `test_news_recall::test_fetch_news_recall_true_merges_and_dedups` | (b) date-brittle | 用例写死 2026-08-09/10,已滑出 `days=30` 滚动 cutoff 窗口 → 采到 0 条 |
| 7 | `test_news_recall::test_fetch_news_recall_false_never_triggers` | (b) date-brittle | 同上 |
| 8 | `test_selection_summary::test_web_selection_analysis_prior_day_picks_real_parse` | (b) date/data-brittle | `_selection_benchmark_at` 兜底解析磁盘 `选股/2026-09-10.md`;该文件随数据累积已存在 → 次日基准被解出、判成"已复盘"而非"待复盘" |

**分类小结**:(a) 网络/环境依赖 = **0**(既有 7 skip 已正确处理);
(b) date/契约脆弱 = 4 条(#5#6#7#8);(c) 真回归 = 4 条(#1 代码新增判据;#2#3#4 同一份
经验库文档被自动提交覆盖)。

## 二、改了什么

### 测试鲁棒化 /(b)类
- **#6#7 `tests/test_news_recall.py`**:引入模块级 `_RECENT = today−2`,把两处写死的
  新闻日期改成相对今天的近日期,恒落在 `days=30` 窗口内。锁的仍是"三源并集 + 扩召回并入
  去重 / recall=False 不触发"语义,不是具体日历日。
- **#5 `tests/test_news_recall.py`**:用例绕过 `__init__` 处补 `cli._disable_thinking = True`,
  对齐 client 重构后的实例属性契约;仍真断言 `extra_body={'enable_thinking': False}` 被下传。
- **#8 `tests/test_selection_summary.py`**:桩住 `da._selection_benchmark_at`,基准只从(已桩)
  `daily_mean` 取,不落 `选股/<date>.md` 文件兜底。锁"次日广度未产出→待复盘",与磁盘上
  恰好有没有次日选股 md 无关。

### 白名单显式定性 /(c #1)
- **`tests/test_exchange_single_source.py`**:把 `factor_nextday.py`、
  `selection_alpha/nextday_kernel.py` 两处 `board_limit` 收入"防复发"白名单并写明理由——
  它们判的是**次日涨跌停幅度上限**(创业/科创 20% vs 主板 10%),与已在册的
  `backtest_midday_q`、`backtest_newhigh._is_20cm` **同构**,属涨跌停口径不是"代码→交易所"
  路由,`tools/config/exchange.py` 也无该谓词可委托。**不改业务代码、不弱化闸门**——闸门本就
  是为"新增独立前缀判据先显式定性"而设,本次正是走了这条定性流程。

### 数据恢复 /(c #2#3#4)
- **`docs/每日分析/经验沉淀/v2026-09-14.md`**:按 `aaf1a60`(手动定稿 commit)恢复被误删的
  3 处锁定 hunk(#31 放量收阳首入场闸门/MA5 降加仓档/标已验证;#10 ⑤capitulation β 例外
  + ⑥弱市不硬凑/标已验证;changelog SOP 追加行)。**只恢复这 3 处、不动其它内容**。

## 三、根因:历史预存 vs 本轮引入

- **本轮引入(c)**:
  - #1:板块预测/选股α 本轮新增 `board_limit` 判据 → 触发防复发闸门。
  - #2#3#4:2026-09-14 手动定稿 `aaf1a60` 后,**同日**自动分析提交 `082357d`
    (`docs(分析师): 自动提交…2026-09-14`)从**陈旧基线**重跑并入,整体覆盖回退了 `aaf1a60`
    的锁定条款。v2026-09-15/16 已正确携带该锁定(propagation 未受影响),**只有 09-14 那份
    被同日自动提交打回**——doc-lint 如实报红。
- **历史预存(b)**:
  - #6#7#8 date-brittle 属"写测试时用了当时的自然日/当时磁盘还没有的次日文件",随时间/数据
    累积必然滑红,与本轮改动无关。
  - #5 是 client 重构(实例属性 `_disable_thinking`)后用例契约未跟进,属重构侧历史欠账。

## 四、修后全量结果

见分支 HEAD;全量 `pytest tests/` 目标回到"绿 或 仅剩既有 LLM/网络 skip"。
(见文件末/回执:passed/skipped/failed 实测数)

## 四·补、顺带修的测试卫生问题(非红,但会脏工作区)

全量跑后发现 `config/strategy.json`(git 跟踪)被改脏——`test_strategy_json_sync` 直接
`strategy.dump_json()` 写**真实** config/strategy.json。用例本身**绿**(dump 后读回自洽),但
有落盘副作用:磁盘那份一旦滞后于 py 阈值,跑一次测试就把工作区改脏、甚至可能被误提交。
已改 hermetic(落 tmp_path、断言不变),该用例不再脏工作区。

**衍生发现(留统筹/config owner)**:本机实测 `strategy.dump_json()` 的导出与 committed
`config/strategy.json` **已漂移**(如市场状态因子少了"科技共振"、五档阈值 20/40/80→25/45/75
等)——说明 `strategy.py` 的 THRESHOLDS 改过但导出 json 没重生成提交,或存在有意分叉。
此 self-consistent 用例**不把守** committed-json↔py 一致(两者都不查),需 config owner
显式 `python -m tools.config.strategy` 重生成并提交,或确认分叉是否有意。本窗未改该 json。

## 五、留给统筹的流水线侧问题(超出本窗范围,不自行动)

**自动 EOD 分析提交会从陈旧基线覆盖手动定稿**(本次 `082357d` 打回 `aaf1a60`)。这不是
测试问题,是 eod-review/自动提交作业的**并版基线**问题:手动在经验库定稿的锁定条款,若当日
稍后的自动提交基于更早基线重跑,就会被悄悄回退。建议统筹在流水线侧核查"自动提交前是否
rebase/consume 最新版",避免锁定条款反复被打回。**该作业属定时任务侧,本窗未触碰。**

## 关键文件
- `docs/每日分析/经验沉淀/v2026-09-14.md`(恢复)
- `tests/test_news_recall.py`、`tests/test_exchange_single_source.py`、
  `tests/test_selection_summary.py`(鲁棒化 / 白名单)
