# 开发日志 · 2026-08-12 · 远端采集架构 + 全A多策略闭环 + 数据源修复

> 本轮跨度大,分多块。按"做了什么 / 关键决策 / 待办与坑"留痕,便于转交。⚠️ 非投资建议。

## 一、本轮交付(已全部 merge 到 main + push origin)

1. **自选池 +3**:行云科技(300209)/国瓷材料(300285)/胜宏科技(300476),池=11。
2. **修 K线陈旧 bug**:`master_sync._fallback` 抓了新数据只写 raw、不推进主档,而 `load_kline` 主档优先→读旧数据。修:回退后把 raw 尾部新 bar 增量 append 进主档。
3. **新增新浪个股新闻源**:东财∪新浪∪财联社(财联社改"总是查"),三源去重合并;补东财稀疏票。
4. **LLM 抽取提速 6.87x**:逐条抽取改有界线程池并行(workers=8)+ 单票送 LLM 上限 40 条 + 抽取 schema 9→6 字段 + 关思考开关。
5. **补数据缺口**:股息率(baostock TTM 现金分红/现价)接入多因子;RRG 行业名映射对齐(紫光/胜宏不再弃权);北向个股明细(港交所 2024-08 停披露)加新鲜度护栏、优雅降级。
6. **策略编号定稿**:策略0=全A合议、1=趋势深跌反包(S01)、**2=放量后缩量回踩(S02,新)**、**3=箱体形态(新)**、**4=动量组合(原策略2 改号)**。新增全A screener:`screen_s02` / `screen_box` / `screen_momentum`(动量仅 A 腿,B 腿需基本面留 web 自选池)。
7. **`screenall` 编排**(`tools.run screenall [--no-fetch] [--no-llm]`):全A 跑 5 策略 → 选出并集 → **只对(并集∪自选)做新闻/LLM/合议**(省 token 命门)。`--no-fetch` 跳过 master_sync(数据已备时免误触 baostock 全量回填)。
8. **web**:选股页 策略2/3/4 改读全A预落盘 view(对齐 0/1),不再自选池实时算;综合选股 5 勾选框并集。
9. **远端采集架构**:`stock-fetch.timer` 从未 enable(disabled)→ 启用 + 改 2×/day(午休 11:35 / 收盘 15:20);本地 `pull_refresh.sh` launchd(周一至五 15:40:pull 全A → screenall --no-fetch → 上传)。
10. **tencent 源修复(关键)**:`_fetch_tencent` 改直连 `web.ifzq.gtimg.cn/fqkline`(当日盘后即含当天 bar;akshare `stock_zh_a_hist_tx` 滞后~1交易日,同日选股拿不到当天)。volume 手→×100 股对齐口径。**顺带全A采集 35min→5min**。
11. **新闻扩召回 + LLM 相关性初筛**:按股票行业动态生成主题关键词(自选池手工 sector 优先、全A baostock 证监会兜底)→ 东财按词召回(候选 cap 30)→ **LLM 中档初筛**(关思考+并行+缓存;留 点名本股/政策管制监管明确冲击主营/直接上下游重大;滤 纯概念异动·涨停/其他个股/泛大盘)→ 并入个股新闻(`fetch_news(recall=True)`,只对 llm_subset 生效)。**实测抓到"行云算力卡海关"澄清消息**(此前盲区)。
12. **LLM 配置精简**:只认 `LLM_BASE_URL`+`LLM_API_KEY`,model 写死 `deepseek-v4-pro`。
13. **两个 bug**:①全A选出票名称显示成代码(serialize 无池名时 meta.name=code)——serialize 从 code_name.json 补名 + web `_resolve_name` 遇 meta.name==code 走 code_name.json;②扩召回覆盖了条目原始 source("扩召回:算力")——改为保留原始来源,扩召回来路由 meta.source contributors 体现。

## 二、关键决策(供后续对齐)
- **策略2 命名撞车**:提供者的 S02「放量后缩量回踩」= 策略2;合作者的「动量组合」→ 策略4。
- **回测前提**:S01/S02 只做"信号日收盘机械基线"回测(标注"非最终买法");S01 因买点主观(低吸/追)不再据此定论。S02 基线:胜率 31.6%/Alpha+0.14%(子集,不足定论)。
- **扩召回尺度 = 中档**(非宁严/宁宽):补管制盲区又不被概念噪声带偏。
- **远端东财被墙**(阿里云也墙)→ 全A 采集走 tencent fqkline;北向源已停披露。

## 三、待办与坑(未决)
1. **全A 行业源失真**:baostock 证监会分类粗、转型股会错(行云=零售)→ 扩召回对"全A 非自选池票"召回弱(自选池用手工 sector 规避)。可考虑接申万行业。
2. **ingest 429 限速**:分片 >120/60s 首轮部分 429(已用"两轮上传"规避)。根治:提高 `SYNC_RATE_MAX` 或上传端分批限速。
3. **LLM 配额**:当前 key(末4 Zl0=)有配额,但本轮用量大;需盯 `~/.local/state/stock/pull_refresh.log` 有无 quota 报错。
4. **ops/systemd 模板未校准**:模板写 `/srv`+`deploy`,实际部署是 `/home/admin`+`admin`;补部署说明避免误导。
5. **策略1 低吸客观买点 + 盘中回测**:需分钟数据,挂起。
6. **S02 全A完整回测**:目前只子集(427只),要定论需全A轮。
7. **momentum.py 悬空引用**:docstring 提到已删的 `web/data_access._store_closes_loader`,小清理。
8. **午休 vs 收盘**:日线选股需收盘价;午休那次采集对日线 screener 是盘中预览,收盘那次(15:20/15:40)才是正式。

## 四、远端/自动化现状
- 远端 `39.105.83.174`:web:8801 / ingest:8802;autoupdate 每小时 git-pull;stock-fetch 2×/day 采全A。
- 本地 launchd:`com.stock.pullrefresh` 周一至五 15:40 全流程。
- 数据日期截至 08-12(盘中版;15:40 收盘版会覆盖)。
