# 问题台账

> 格式见 [开发规范.md](../开发规范.md) 第三章 (b)。
> 待决项按状态维护;bug 踩坑按诊断链闭环后归档到本文下半部。

## 一、待决问题(open)

| 编号 | 标题 | 状态 | 提出 | 说明 |
|---|---|---|---|---|
| Q1 | 重点票筛选规则 | 待拍板 | 08-05 | 涨幅异动 / 舆情热度 / 重大事件 / 综合(hybrid)。建议 hybrid。影响 `analysis/portfolio.pick_focus`。 |
| Q2 | 是否需要定时跑 | 待拍板 | 08-05 | 每日盘后 vs 一次性。建议先一次性,P5 再加调度。影响是否做 P5。 |
| Q3 | UGC 先抓雪球还是股吧 | 待拍板 | 08-05 | 都抓成本高。建议先只抓东财股吧(反爬低、与数据源同源)。影响 `collectors/ugc`。 |
| Q4 | 报告产出是否进 git | 待拍板 | 08-05 | 建议不进(已 gitignore `docs/报告/`),避免每天报告污染仓库。 |
| Q5 | 自选股截图是否入库 + 仓库公私 | 待拍板 | 08-05 | 截图暴露个人持仓。若仓库公开则截图不入库(留文字版股票清单即可)。**push 前必须确认**。 |
| Q6 | 首次 push 时机 | 待拍板 | 08-05 | 现已本地提交 P0(`071c723`),未 push。等 Q5 确认 + 架构审阅通过后再 push。 |

## 二、外部依赖风险(持续跟踪)

| 编号 | 风险 | 状态 | 说明 |
|---|---|---|---|
| R1 | akshare 接口随平台改版变动 | 跟踪中 | 函数名/参数会变,落地前以官方文档实测为准;不硬编码脆弱字段。 |
| R2 | Tushare Pro 曾突发停运 | 跟踪中 | 不单点依赖,akshare 为主 + 多源冗余(见数据源调研)。 |
| R3 | 同花顺问财 JS 加密、雪球需登录态 | 跟踪中 | UGC 抓取反爬成本高,只对目标票池小批量抓,能落 akshare 就不自建爬虫。 |

## 三、bug 踩坑记录(闭环归档)

> 暂无。P1 起,凡走过"诊断链"的 bug 在此归档:现象/日志 → 范围 → 根因 → 方案 → 预期效果 → 结果 → 是否锁进回归测试。
