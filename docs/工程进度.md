# 工程进度（功能-模块-需求-状态）

> **本文用途**：全项目进度的**活文档**——一张总表说清 **目前实现了哪些功能 / 设计了哪些模块 / 关联的需求文档是哪个 / 状态到哪了**。
> **状态枚举（固定四个词，别自创）**：`构建中`（骨架已立/规划中待填）· `开发中`（正在写）· `已完成`（已落地可用）· `已废弃`。
> **更新**：2026-08-19(**半导体多因子(策略E/6)打通数据链**:加 `tools/pipeline/screen_semi_factor.py` 扫半导体池 178 只(fetch=True 自采 fundamental/financial/kline)→ 落 view「半导体多因子」;接进 `run.run_screen_all` 6 策略列表(半导体池票自动进 `union_picks` → 全套 LLM/合议/横表);web 层策略6 改"view 优先 + live 兜底"(输出 `source: view|live`);136 项相关测试全绿。远端下轮闭环自动出结果。详见 [开发日志_20260818_半导体多因子策略移植](计划/开发日志_20260818_半导体多因子策略移植.md) 顶部"补丁"段。)· 2026-08-18(**SEPA+VCP 监控落地**:计算核 `analysis/sepa_vcp/` + `run sepa` 两表雷达 + Web `/sepa` + 午间/收盘定时模板;本机 `--no-fetch` 可扫主档;部署手册 [本地部署与监控手册](参考/本地部署与监控手册.md)。不是选股页「策略5」。)· 2026-08-18(**移植聚宽两条策略 + web 上页**:①**小市值** 分析层新增 策略C_小市值组合(全A中小板指历史成分股943只+剥30/68+次新过滤)/ 策略D_自选池小市值组合(不限池不剥板块关次新,适配自选池13只);沿用 `8650081` 剥离规矩,不代买ETF;空仓月`embargo`标记透传。②**半导体多因子** 新增 策略E_半导体多因子(**限申万二级 801081 半导体池 178 只** + rd/rev × 0.6 + rd/mcap × 0.2 + 营收增速 × 0.2 winsor+zscore 加权;数据变通:从 financial.derived 反算 rd 无需新采;本机自选池 ∩ 半导体池=∅,远端全A闭环采到后即出结果)。**选股页 `/selection` 综合选股面板扩到 7 策略**:0/1/2/3/4 全A view + 5(小市值,自选池实时)/ 6(半导体多因子,限半导体池实时);`embargo=True` 显示"空仓月标记"badge。106 项相关测试全绿;详见 [开发日志_20260818_小市值策略移植](计划/开发日志_20260818_小市值策略移植.md) + [开发日志_20260818_半导体多因子策略移植](计划/开发日志_20260818_半导体多因子策略移植.md))· 2026-08-13(**预测两条线(止跌/止盈)首次历史回测验证 + 调参定稿**：随机全A 29万观测,上涨概率被证纯噪声、区间校准到80%、L3/括号止损调参;`strategy.py['预测']` 参数落地。**策略 0/3/4 首次回测**(信号型 `backtest_box` / 排序型 `backtest_rank`）:箱体弱正不显著(Alpha+0.32%)、动量A腿 IC 显著为负(A股反转)、合议(技术面)无 edge;追反转因子=账面真·**不可交易**。**总结论:纯量价·日频无可部署正 alpha**。详见 [策略总览](策略/策略总览_定义计算与回测.md) + [开发日志_20260813](计划/开发日志_20260813_预测验证与策略回测总盘点.md)）· 2026-08-07（补 V1 形态选股 MVP 落地：指数/板块/全A票池采集 + pattern_screener 策略包 + 全A扫描编排 + 双层RS，余护栏/市场状态/前瞻回测/全量首扫见后续批次计划；补展示端 B 期落地:ingest 收录+钢印+签名上传(P2–P4)+远端自动更新自愈,均合 main,展示端整体标记已完成(余真实上线人工);此前：store DB 后端 + 定时调度 + 图表库选型 + 远端部署 A 期 + 页面端口改 8801 + B 期 ingest 开发简报）· **维护规矩**：任何开工/收工的窗口都要更新本文；**用户约定：每次汇报进度前先把本文更新到最新**。
> 相关文档：详细需求见 [计划/](计划/)（= 需求文档，按 [模板](计划/_需求文档模板.md) 写）；已完成流水见 [开发日志](日志/开发日志.md)；待决/踩坑见 [问题台账](问题/问题台账.md)；边界/依赖见 [架构设计.md](架构设计.md)。

---

## 一、功能-模块-需求-状态 总表

> 列含义：**功能**=做的事 ｜ **涉及模块**=代码位置（`tools/`·`web/`）｜ **关联需求文档**=对应 `docs/计划/` 或模块 `需求.md`（无则标 —）｜ **状态**=四枚举之一。

### 采集层（唯一外部 I/O 边界）

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| 行情/K线采集 | `tools/collectors/market.py` | — | 已完成 |
| 基本面采集（营收/净利/ROE/估值） | `tools/collectors/fundamental.py` | [P2_结构化情绪与基本面](计划/P2_结构化情绪与基本面.md) | 已完成 |
| 公告采集（巨潮） | `tools/collectors/announcement.py` | — | 已完成 |
| 资金流采集（东财） | `tools/collectors/fundflow.py` | — | 已完成（8 只限流待补，见待做） |
| 新闻采集（东财/财联社备源） | `tools/collectors/news.py` | [P2C_新闻情绪LLM](计划/P2C_新闻情绪LLM.md) | 已完成 |
| 政策采集（关键词检索+行业打标） | `tools/collectors/policy.py` | — | 已完成 |
| 舆情 UGC 采集（东财股吧 curl_cffi） | `tools/collectors/ugc.py` | — | 已完成 |
| 指数采集（宽基/主要指数日K线，新浪→东财 fallback） | `tools/collectors/index.py` | [V1_形态选股与市场状态系统](计划/V1_形态选股与市场状态系统.md) | 已完成（沪深300 实测通，供 RS 基准+市场状态指数因子） |
| 板块采集（申万一级指数K线 + baostock 个股→行业成分映射） | `tools/collectors/board.py` | [V1_形态选股与市场状态系统](计划/V1_形态选股与市场状态系统.md) | 已完成（东财板块 kline/clist 路径本机被网络墙→板块指数走申万、成分走 baostock 证监会行业） |
| 全 A 票池采集（akshare，排除北交所） | `tools/collectors/universe.py` | [V1_形态选股与市场状态系统](计划/V1_形态选股与市场状态系统.md) | 已完成（5539 只可达） |

### 分析 / 策略层（纯计算，文本侧调 LLM）

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| 技术判定（趋势评级/拐点/超买超卖） | `tools/analysis/technical.py` | [P1_技术面打通](计划/P1_技术面打通.md)·[P1.5_拐点信号增强](计划/P1.5_拐点信号增强.md)·[P1.6_结构化输出与指标增强](计划/P1.6_结构化输出与指标增强.md) | 已完成 |
| 估值判定 | `tools/analysis/valuation.py` | — | 已完成 |
| 价位预测（支撑压力/止盈止损/情景） | `tools/analysis/predict.py` | [P3_Web展示与预测引擎](计划/P3_Web展示与预测引擎.md)·[预期价位预测](策略/预期价位预测.md) | 已完成（**2026-08-13 首次回测验证+调参定稿**：参数按全A回测落地；⚠️「上涨概率」经证无区分力=不当胜率，页面待弱化） |
| 买卖倾向决策（多因子含情绪） | `tools/analysis/predict.py` | [P3_Web展示与预测引擎](计划/P3_Web展示与预测引擎.md) | 已完成 |
| 情绪三层（新闻/政策/舆情 LLM 打分→加权净情绪） | `tools/analysis/event.py`·`sentiment.py`·`tools/llm/` | [P2C_新闻情绪LLM](计划/P2C_新闻情绪LLM.md) | 已完成 |
| 策略层（选股/评分/信号 可注册 `@strategy`） | `tools/strategy/registry.py`·`momentum.py`·`small_cap.py`·`semi_factor.py` | — | 已完成(已移植聚宽 5 策略:A/B 动量组合 · C 全A小市值 · D 自选池小市值 · E 半导体多因子(限申万二级 801081 池)) |
| 财报深度分析（盈利质量/成长/现金流…） | `tools/analysis/financial/`（`metrics`/`flags`/`scoring`/`analyzer`/`industry/`） | [架构设计](财报分析专家/架构设计.md)·[开发日志20260818](计划/开发日志_20260818_行业财报专家框架.md) | 数值层可跑；行业专家框架落地（7 行业插拔覆写 + 自动发现注册表，218 测试）；阈值/区间占位待标定，银行监管附注比率待补采 |
| 形态选股策略包（形态引擎4形态 + RS双层 + 硬规则AND + 全市场达标占比） | `tools/analysis/pattern_screener/`（`pattern`/`rs`/`screen`） | [V1_形态选股与市场状态系统](计划/V1_形态选股与市场状态系统.md) | 已完成（MVP 可跑；形态几何参数占位待策略端标定；RS 双层用同业等权均值代理板块指数） |
| **SEPA+VCP 监控**（全A均线入池 + 波段收缩观察 + 两表雷达，非交易） | `tools/analysis/sepa_vcp/` · `pipeline/screen_sepa_vcp.py` · `web` `/sepa` | [SEPA_VCP监控模块_需求](计划/SEPA_VCP监控模块_需求.md) | **已完成**（本机 CLI/Web/定时模板可用;占位阈值待肉眼标定。不是选股页「策略5」。手册:[本地部署与监控手册](参考/本地部署与监控手册.md)） |

### 组装 / 契约层

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| 中心记录组装（过契约校验） | `tools/analysis/serialize.py` | — | 已完成 |
| 数据契约（schema/枚举/校验器） | `tools/contracts/` | — | 已完成 |

### 聚合 / 选股 / 回测层

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| 组合层共性汇总（池级维度） | `tools/analysis/portfolio.py` | — | 已完成 |
| 横向面板总表（panel csv/json） | `tools/analysis/panel.py` | — | 已完成 |
| 选股筛选（预设 filters） | `tools/screener/screen.py` | — | 已完成（N1 规则留口子待拍板） |
| 回测 BT.1 信号回测（walk-forward 防未来函数） | `tools/backtest/engine.py`·`metrics.py` | [回测层](计划/回测层.md)·[需求](../tools/backtest/需求.md) | 已完成 |
| **预测两条线回测（5指标事件研究 + OFAT调参）** | `tools/backtest/backtest_predict.py`·`tests/test_backtest_predict.py` | [预测两条线_回测验证与调参](计划/预测两条线_回测验证与调参_计划.md) | 已完成（随机全A 29万观测;止损触发率/止盈先命中/区间校准/上涨概率校准/叠加方向期望;参数已定稿写回 `strategy.py['预测']`） |
| **策略回测·信号型（S01/S02/箱体，持仓状态机）** | `tools/backtest/backtest_box.py`·`backtest_s02.py`·`position_backtest.py` | [策略0_3_4_回测方案](计划/策略0_3_4_回测方案与预注册预期.md) | 已完成（箱体 N=1415：胜率31%/Alpha+0.32%/即死61%，弱正不显著） |
| **策略回测·排序型（动量/合议，IC/分层/TopK）** | `tools/backtest/backtest_rank.py`（打分可插拔+反转rev*+流动性过滤+多空净成本） | [策略0_3_4_回测方案](计划/策略0_3_4_回测方案与预注册预期.md) | 已完成（动量A腿 IC 显著为负=A股反转；合议 IC≈0 无edge；反转账面真·不可交易） |
| 回测 BT.2 选股回测（需历史时点记录快照） | `tools/backtest/`（待扩） | [回测层](计划/回测层.md)·[需求](../tools/backtest/需求.md) | 构建中（缺历史 records 快照工程；排序型回测已用切片重放绕开） |
| ML 预测 + 回测 | `tools/backtest/`（未来一策略） | [P3E_ML预测与回测](计划/P3E_ML预测与回测.md) | 构建中（已设计待启动，需统计基线） |

### 基座层

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| 配置/票池/策略阈值（单一真源） | `tools/config/` | — | 已完成 |
| 数据存取 store（raw/记录/视图统一读写） | `tools/store/repo.py` | [存储层收敛与数据新鲜度](计划/存储层收敛与数据新鲜度.md) | 已完成 |
| 按日期分区存储 + 10 只开发样本 + 新鲜度 API | `tools/store/repo.py`·`config/` | [按日期存储与10只样本流水线](计划/按日期存储与10只样本流水线.md) | 已完成（load_X 接 is_stale 告警待做） |
| store **DB 后端**（SQLite 起步，可切 MySQL/Postgres；`STORE_BACKEND`/`DB_URL` 配置切换） | `tools/store/backend_db.py`·`repo.py`（分析侧分发；raw 恒文件） | [展示端与数据同步](计划/展示端与数据同步.md) | 已完成（P1；与文件后端逐字段等价回归，web 零改动读库） |
| LLM 客户端（deepseek-v4-pro @ 内部网关） | `tools/llm/client.py`·`prompts.py` | — | 已完成 |

### 编排层

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| CLI 全链路编排 | `tools/run.py` | — | 已完成（含 `pattern` / **`sepa`** `--session 午间|收盘` `--no-fetch`） |
| 形态选股全A扫描（取数→RS→硬规则→达标占比→落 view「形态选股」） | `tools/pipeline/screen_pattern.py` | [V1_形态选股与市场状态系统](计划/V1_形态选股与市场状态系统.md) | 已完成（端到端跑通；RS 双层/逐票降级/护栏待接线均写进 view 降级字段） |
| **策略5·半导体多因子** 全A扫描（限申万二级 801081 池 178 只，fetch=True 自采 fundamental/financial → 3 因子 winsor+zscore 加权 → 落 view「半导体多因子」） | `tools/pipeline/screen_semi_factor.py` · `tools/collectors/semi_universe.py` · `tools/strategy/semi_factor.py` | [开发日志_20260818_半导体多因子策略移植](计划/开发日志_20260818_半导体多因子策略移植.md) | 已完成（数据链接入 `run_screen_all` 6 策略;远端下一轮闭环自动出结果） |
| 编排 DAG（显式阶段依赖/失败隔离） | `tools/pipeline/pipeline.py` | [需求](../tools/pipeline/需求.md) | 构建中（骨架 `NotImplementedError` 待填，勿重建） |
| 能力注册表（JSON-in/out 供 Agent 调用） | `tools/registry/registry.py` | [需求](../tools/registry/需求.md) | 构建中（骨架待填，勿重建） |
| 定时调度（进程内 APScheduler；T1 盘后全量 / T5 兜底补数 / 可选 SEPA 午间+收盘） | `tools/scheduler.py` | [架构设计 §8.2](架构设计.md) | 已完成（间隔走 env；`SCHED_SEPA_ENABLED` 注册工作日 11:35/15:35。本机更推荐 launchd 见手册） |

### 展示 / 交付层（只读中心记录，不重算/不触网）

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| Web 四页（首页/选股/新闻/个股 + K线 ECharts） | `web/app.py`·`data_access.py`·`templates/` | [P3_Web展示与预测引擎](计划/P3_Web展示与预测引擎.md) | 已完成(选股页 `/selection` 综合选股勾选面板已扩到 7 策略:0/1/2/3/4/5/6;5=自选池小市值,6=半导体多因子(限申万二级 801081 池),均 web 层实时跑) |
| **Web【SEPA监控】页** | `web/app.py` `/sepa`·`sepa/{code}`·`data_access.sepa_*`·`templates/sepa*.html`·`static/sepa_chart.js` | [SEPA_VCP监控模块_需求](计划/SEPA_VCP监控模块_需求.md) | 已完成（只读 view;标题禁止「VCP 完成」） |
| **Web【选股分析】页(定向分析报告展示)** | `web/app.py` `/selection-analysis`·`data_access.list/get_analysis_report`·`templates/selection_analysis.html` | — | 已完成（读 `data/reports/选股分析/*.md`,markdown 渲染,文件名白名单;打通"指定股票→离线分析→网页展示"闭环;依赖 `markdown` 库） |
| Web 三层情绪面板动态展示 | `web/`·`templates/` | [P2C_新闻情绪LLM](计划/P2C_新闻情绪LLM.md) | 已完成 |
| Web 历史日期下拉 + 新闻列表/详情页 | `web/app.py`·`templates/` | [新闻AI评论与交互改进](计划/新闻AI评论与交互改进.md) | 已完成 |
| 新闻 AI 评论 + 统一新闻存储格式（news_ai）+ 面包屑/返回交互 | `tools/analysis/news_ai.py`·`web/` | [新闻AI评论与交互改进](计划/新闻AI评论与交互改进.md) | 已完成 |
| Markdown 报告（个股/汇总，产出到 `data/reports/`，不入库） | `tools/report/builder.py`·`analysis/panel.py` | — | 已完成 |
| 远端展示端部署（A 期：git 携带产物→导入 DB→`STORE_BACKEND=db` 只读展示；含 nginx/HTTPS/systemd runbook）+ 文件产物导入 DB 工具 | `tools/sync/import_to_db.py`·[部署说明.md](参考/部署说明.md) | [展示端与数据同步](计划/展示端与数据同步.md) | 已完成（导入器幂等+等价读回，端到端四页从 DB 200；远端不采集/不跑 LLM/不跑定时） |
| 展示端 ingest 收录 + 钢印 + 本地签名上传（B 期）：token 鉴权→HMAC 验签→防重放→时效→契约→幂等落库→审计;本地打包/签名/分片重试/断点续传 | `tools/sync/{ingest,sign,audit,upload}.py`·[部署说明.md](参考/部署说明.md) | [B 期 ingest 开发简报](计划/展示端B期_ingest与上传_开发简报.md) | 已完成（sync 27 例;ingest 独立 8802,落库走 store 公开 API,零碰 store/web;余真实上线人工步骤见部署说明 B 节） |
| 远端自动更新 + 自愈（拉取→装依赖→重启→健康检查失败回滚 + systemd timer 定时 + runbook） | `ops/remote_update.py`·`ops/systemd/`·[远端自动更新与自愈](参考/远端自动更新与自愈.md) | [B 期 ingest 开发简报](计划/展示端B期_ingest与上传_开发简报.md) | 已完成（ops 16 例;逻辑可注入单测,systemd 安装/触发为远端人工步骤） |
| 本地采集机自动化闭环（Mac launchd 盘后全池采集+分析 → B 期签名上传 → 远端 ingest 落库上屏;数据传输弃 git 改上传） | `ops/local_autopush.py`·`ops/launchd/`·`ops/systemd/stock-ingest.service`·[远端自动更新与自愈](参考/远端自动更新与自愈.md) | [展示端与数据同步](计划/展示端与数据同步.md) | 已完成代码（本地 7 例;真机 apply=服务器起 ingest+自签 TLS / 本机 launchd+证书,见部署记录 B 节） |

### 调研 / 技术选型（决策留痕，不落代码）

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| K 线图表库选型：TradingView Charting Library(Advanced Charts) 是否引入 | `web/`（ECharts 现状） | [图表库_TradingView_Charting_Library_调研](参考/图表库_TradingView_Charting_Library_调研.md) | 已完成（结论：**保持 ECharts**；免费授权要求公众免登录访问，与展示端 token 鉴权冲突，能力错配，暂不引入） |
| 多策略合议（专家投票）架构 + 新策略 roadmap | `tools/analysis/council.py`·`experts.py`·`tools/contracts/expert.py`·`config/strategy.py["合议"]`（`docs/策略`⟺registry⟺UI 三对齐） | [设计稿](计划/多策略合议_专家投票架构_与新策略roadmap.md) · [实现需求](计划/多策略合议_实现需求.md) | 构建中（设计稿+9待决点已定；**F1–F8 已实现**：ExpertVerdict 契约+experts 适配器+council 合议层+买卖倾向等价迁移（**2240 组穷举等价回归红线守住**）+ **Web 专家勾选/归因/冲突 + 前端重合成(D7)** + **F6 多因子** + **F7 事件驱动**(PEAD+增减持回购) + **F8 RRG 板块轮动**(RS-Ratio+RS-Momentum 四象限)；8 专家全进默认组;**编排接线已完成**(run.py 全链路端到端打通);**RRG 行业名映射已补**(证监会83门类100%→申万一级,数据齐时发声);**综合分口径优化**(弃权/缺失专家不稀释分母=Σ权重×置信度,F4 等价不受影响);**⚠️ 待跟进:`web/static/council.js` 前端重合成仍用旧分母,需同步为置信度加权防前后端漂移**;余:真实数据端到端验证靠数据积累） |

### 新增需求（已定稿需求文档，待开发）

| 功能 | 涉及模块 | 关联需求文档 | 状态 |
|---|---|---|---|
| 远端展示端 + DB + 上传/收录接口 + 传输安全钢印 | store DB 后端 / `tools/sync/`(ingest·sign·audit·upload·import) / `ops/`(自动更新) | [展示端与数据同步](计划/展示端与数据同步.md) · [B 期 ingest 开发简报](计划/展示端B期_ingest与上传_开发简报.md) | **已完成**（P1 store DB 后端 + A 期 git 携带→导入 DB 只读展示(8801) + B 期 ingest 收录/钢印/签名上传(P2–P4) + 远端自动更新自愈,均已合 main、共 43 例 sync/ops 单测;**唯一剩余=真实服务器上线**(配密钥 env、起 systemd、nginx 挂 HTTPS、装 timer),按部署说明 B 节 + 自愈 runbook 人工执行) |
| 形态选股 + 市场状态系统（V1，同事定稿） | `analysis/pattern_screener/`(含 `regime.py`) · `collectors/{index,board,universe}` · `pipeline/{screen_pattern,regime}.py` | [V1_形态选股与市场状态系统](计划/V1_形态选股与市场状态系统.md)·[形态选股_后续批次](计划/形态选股_后续批次(护栏+市场状态+前瞻回测).md) | **代码全完成**（批次A护栏+正向确认 / B市场状态regime / C前瞻回测harness(regime增益+防未来函数) 均合 main;批次D全量首扫交定时任务）;**余=数据积累(回测快照)+策略端标定(五档边界/龙头池/形态参数/正向确认阈值)** |
| **SEPA+VCP 监控模块**（提供者终稿 2026-08-18） | `analysis/sepa_vcp/` · `pipeline/screen_sepa_vcp.py` · `/sepa` | [SEPA_VCP监控模块_需求](计划/SEPA_VCP监控模块_需求.md) | **已完成**（过滤器+人工翻图;手册 [本地部署与监控手册](参考/本地部署与监控手册.md);占位阈值待肉眼标定） |

---

## 二、状态小结

| 状态 | 项数 | 主要内容 |
|---|---|---|
| **已完成** | 39 | 采集 7 层 + **指数/板块/全A票池采集(V1)** + 技术/估值/预测/情绪三层/策略层 + **形态选股策略包(形态+双层RS+硬规则+达标占比)** + 组装/契约 + 组合/面板/选股/回测 BT.1 + **形态选股全A扫描编排** + store 按日期 + **store DB 后端(SQLite)** + **定时调度(APScheduler)** + LLM + CLI + Web 四页/情绪/历史日期/新闻AI评论 + 报告 + 图表库选型调研（保持 ECharts） + **远端部署(A期)+导入器** + **展示端 ingest+钢印+签名上传(B期)** + **远端自动更新自愈** + **SEPA+VCP 监控(`/sepa`)** |
| **开发中** | 1 | **V1 形态选股+市场状态**（MVP 已合 main;余 护栏接线/P3市场状态/P4前瞻回测/全量首扫） |
| **构建中** | 5 | 财报分析(N2) · 回测 BT.2 · ML 预测回测 · pipeline DAG · registry 注册表 |
| **已废弃** | 0 | — |

> 注：均线金叉信号经 BT.1 实测超额约 -28%（诚实拦截差策略），属**回测结论**而非废弃模块，故不计入"已废弃"。

---

## 三、待做队列（Backlog · 按优先级）

> 有代码骨架的在上表标"构建中"；下面是尚需拍板/排期或补数的细项。

| 优先级 | 任务 | 说明 | 依赖/阻塞 | 预估 |
|---|---|---|---|---|
| P1 中 | SEPA 占位阈值肉眼标定 | pivot 窗 / 距前高% / 星标客观口径,先看 `/sepa` 再改 `THRESHOLDS['SEPA_VCP']` | 模块已可跑,见[需求 3.5](计划/SEPA_VCP监控模块_需求.md) | 小 |
| P2 小 | SEPA `接近枢纽` 语义收紧 | 现 `接近枢纽 = 末轮进行中 + 量枯 + 距前高近`（`vcp.py:196` `analyze_vcp`），**未要求末轮相对上一轮硬收缩**,比终稿「末轮**收缩中**」略宽:只 1 轮的首次回撤也会被标。待标定时考虑加「末轮硬收缩(或 ≥2 轮)」门 | 纯计算改动,不依赖数据;与阈值标定一并做最省 | 小 |
| **P0 高** | 回测 BT.2 选股回测 | 需"历史时点的中心记录快照"（现只存最新一份）→ 补历史 records 快照工程 | BT.1 已就绪 | 大 |
| **P0 高** | 形态选股·护栏接线（批次A） | 编排层 pass1 补取基本面(PE分位/净利增速)+公告喂 `is_qualified`,让负向护栏生效 | `fundamental`/`announcement` 已存在(待验数据源);见[后续批次计划](计划/形态选股_后续批次(护栏+市场状态+前瞻回测).md) | 中 |
| P1 中 | 形态选股·P3 市场状态(批次B) | `pattern_screener/regime.py` 五因子平权+五档标签,宽度吃达标占比,落 market view | 达标占比已就绪;待策略端标定五档边界/龙头池 | 中 |
| P1 中 | 形态选股·P4 前瞻胜率回测(批次C) | 放 `tools/backtest/`:历史达标池→前瞻5/10/20日胜率+Alpha,防未来函数 | 需历史达标池快照(多日积累/回补) | 中 |
| P2 | 形态选股·全量首扫(批次D) | `run pattern --universe` 全5539只跑一遍出真实全市场达标池 | 护栏接线后跑最有意义;纯运维(~40min) | 小(无开发) |
| P1 中 | 情绪并入超买超卖 | 情绪已并入 `predict.买卖倾向`(✅)；进一步并入 `ob_os` 超买超卖 verdict → 组合双维判定，待设计 | sentiment 已在记录 | 中 |
| P1 中 | 命名统一 `ob_os.verdict`→`结论` | 英文键统一到中文，与 trend.评级/买卖倾向.结论 一致；跨 technical/serialize/contracts/web/report | 契约已标注，需回归全测 | 中 |
| P2 | 情绪三层 Web 明细 + 政策/UGC 真跑 | 数据已入 sentiment；政策/UGC 打分需真实跑一轮（LLM 成本） | 已就绪 | 小 |
| P2 小 | load_X 接入 is_stale 告警 | 新鲜度 API 已就绪（`store.is_stale`），各 load_X 读到陈旧缓存时告警 | 存储地基已就绪 | 小 |
| 数据补 | 资金流补缺 8 只 | 东财 fflow 限流(R5)，缺 300394/600498/603019/002837/300073/300748/000539/000601 | 择时/换网重试，不硬刷 | 小 |

---

## 四、阻塞 / 待用户拍板

| 编号 | 事项 | 卡在哪 |
|---|---|---|
| N1 | 选股筛选具体规则 | 待用户给口径（现用默认预设，`screener/` 留口子） |
| N2 | 财报分析框架 | 待用户单独讨论（看什么/产出/哪些用 LLM，`financial_report/` 留口子） |

> 已解锁历史阻塞：Q7 大模型 API（deepseek-v4-pro 已接入）。

---

## 五、与其它文档的分工 + 认领指引

| 文档 | 管什么 | 时态 |
|---|---|---|
| **本文（工程进度）** | 功能-模块-需求-状态 + 待做/阻塞（前瞻/协调） | 现在 + 将来 |
| [开发日志](日志/开发日志.md) | 已完成的事（流水，含决策/踩坑） | 回溯 |
| [问题台账](问题/问题台账.md) | 待决问题 + bug 踩坑闭环 | 专题 |
| [计划/](计划/) = 需求文档 | 单个需求/Phase 的详细方案（动手前写，按[模板](计划/_需求文档模板.md)） | 前瞻（详） |
| [架构设计 §11](架构设计.md) | 演进路线（阶段级） | 战略 |

**认领指引**：从上表挑一项"构建中/待做"（优先 P0）→ 用 [git worktree](开发规范.md#六git-协作与多窗口冲突规避硬约束--已因并发丢过代码) 开独立分支 → 开发+自测 → push → 写开发日志 → 合 main → 回本表更新状态。全程守 [开发规范.md](开发规范.md)（尤其 §六 Git 铁律：main 只做集成、一分支一写者、并行用 worktree）。
