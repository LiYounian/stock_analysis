# 工程进度(任务看板)

> **本文用途**:全项目的**工程进度(已做/在做/待做)+ 任务分派**的**活文档**。开工前先在这里认领,避免多窗口撞车。
> 结构:进行中 · 待做队列 · 模块开发包(并行分配)· 阻塞待拍板 · 已完成里程碑。
> 更新:2026-08-06(核对模块树,标注 pipeline/registry/financial_report/sentiment 骨架已搭)· 维护:任何开工/收工的窗口都要更新本文。**用户约定:每次汇报进度前先把本文更新到最新,避免重复问。**

## 与其他文档的分工(别放错地方)

| 文档 | 管什么 | 时态 |
|---|---|---|
| **本文 任务看板** | **谁在做什么 + 进度 + 待做队列**(前瞻/协调) | 现在进行 + 将来 |
| [开发日志](日志/开发日志.md) | 已完成的事(流水,含决策/踩坑) | 回溯 |
| [问题台账](问题/问题台账.md) | 待决问题 + bug 踩坑闭环 | 专题 |
| [计划/](计划/) | 单个 Phase 的详细方案(动手前写) | 前瞻(详) |
| [架构设计 §11](架构设计.md) | 演进路线(阶段级) | 战略 |

## 使用规矩(对齐 [开发规范.md](开发规范.md) 第六章 Git 铁律)

1. **开工前**:把要做的任务从「待做」挪到「进行中」,填**窗口/负责人、worktree 路径、分支名、开始时间**。→ 别人看到就不碰同一分支/文件。
2. **并行开发用 git worktree**(开发规范 §六 规则 0):新任务在 `claude_dir/worktree/<任务名>` 开独立 worktree + 分支,各窗口 HEAD 独立、不互相打断;主窗口(主仓)只在 main 做集成。
3. **完成后**:从「进行中」挪到「已完成(近期)」,并在 [开发日志](日志/开发日志.md) 写详情、合并 main、删分支。
4. **阻塞/待用户拍板**的进「阻塞区」,注明卡在什么。
5. **本文本身也是共享文件**:更新时小步、及时提交,避免与他人同时改(纯追加/改自己那行最安全)。

---

## 一、进行中(In Progress)

| 任务 | 负责窗口/人 | 分支 | 开始 | 状态 |
|---|---|---|---|---|
| —(当前无人认领,从下方「待做」认领)— | | | | |

## 二、待做(Backlog · 按优先级)

| 优先级 | 任务 | 说明 | 依赖/阻塞 | 预估 |
|---|---|---|---|---|
| P1 中 | 情绪面并入超买超卖 | 情绪已并入 `predict.买卖倾向`(✅);进一步把情绪并入 `ob_os` 超买超卖 verdict 需在有情绪的层做(technical 只见 kline)→ 组合双维判定,待设计 | sentiment 已在记录 | 中 |
| **P0 高** | 回测层 BT.2 选股回测 | BT.1 信号回测已落地;**选股回测**需要"历史时点的中心记录快照"(现只存最新一份)→ 补历史 records 快照工程 | BT.1 已就绪;需历史快照 | 大 |
| P1 中 | 命名统一 | `signals.ob_os.verdict`(英文键)统一到中文 `结论`,与 trend.评级/买卖倾向.结论 一致 | 跨 technical/serialize/contracts/web/report 多模块,契约已标注,需回归全测 | 中 |
| P2 | 情绪三层 Web 展示 | 情绪三层(新闻/政策/舆情)数据已入 sentiment;Web 个股页情绪面板可展示三层明细 + 政策/UGC 打分需真实跑一轮(LLM 成本) | 已就绪 | 小 |
| P2 规划 | run.py 变薄 → pipeline.py | 业务编排下沉为显式 DAG,run.py 只做 CLI(架构 §8.1/§9.1);**骨架已搭**(`pipeline/pipeline.py` Stage+run_pipeline 待填,勿重建) | 架构标注"规划,暂不开发" | 中 |
| P2 规划 | registry.py 能力注册表 | 每能力登记为 JSON-in/out 工具,供 Agent 调用(架构 §6.4);**骨架已搭**(`registry/registry.py` capability/list/call 待填,含双模设计,勿重建) | 规划 | 中 |
| P2 规划 | 定时调度 | cron T1–T5 分层(架构 §8.2);LLM 类单独调度控成本 | 规划,依赖 pipeline | 中 |
| P2 第二阶段 | ML 预测 + 回测 | 见 [计划/P3E_ML预测与回测.md](计划/P3E_ML预测与回测.md);回测框架先行防过拟合 | 需统计基线跑一段 | 大 |
| 数据补 | 资金流补缺 8 只 | 东财 fflow 限流(R5),缺 300394/600498/603019/002837/300073/300748/000539/000601 | 择时/换网重试,不硬刷 | 小 |

## 二·五、模块开发包(并行分配用 —— 每个包=独立位置+需求文档,拿了就能开工)

> 解耦架构下,**新文件夹模块**天然隔离、可放心并行(多 Agent/多人各拿一个包);**改现有共享文件**的任务需协调/串行(标注在"并行"列)。开工前在「进行中」认领。

| 模块 | 开发位置 | 需求文档 | 层 | 依赖 | 分支建议 | 状态 | 并行安全 |
|---|---|---|---|---|---|---|---|
| 回测层 | `tools/backtest/` | [需求](../tools/backtest/需求.md) | 聚合同级 | strategy · store | `feat/backtest-bt1` | ✅ BT.1已落地(BT.2选股待做) | ✅ 独立 |
| 编排 pipeline | `tools/pipeline/` | [需求](../tools/pipeline/需求.md) | 编排 | run.py 入口 | `feat/pipeline` | 规划 | ✅ 独立 |
| 能力注册表 | `tools/registry/` | [需求](../tools/registry/需求.md) | 基座 | 各能力 | `feat/registry` | 规划 | ✅ 独立 |
| 财报分析 | `tools/financial_report/` | [需求](../tools/financial_report/需求.md) | 分析 | fundamental · llm | `feat/financial-report` | 🔒 N2 阻塞 | ✅ 独立 |
| 政策采集 | `tools/collectors/policy.py` | 该文件 docstring | 采集 | 东财新闻 · llm | `feat/collector-policy` | ✅ 采集已落地(情绪打分待接) | ✅ 独立 |
| 舆情 UGC 采集 | `tools/collectors/ugc.py` | 该文件 docstring | 采集 | 东财股吧(curl_cffi) | `feat/collector-ugc` | ✅ 采集已落地(情感打分待接) | ✅ 独立 |
| 情绪并入决策 | `tools/analysis/predict.py`(改) | 见待做 P0 说明 | 分析 | sentiment(已通) | `feat/sentiment-into-decision` | ✅ 可开工 | ⚠️ 改 predict,单写者 |
| 命名统一 | 跨 technical/serialize/contracts/web | 契约已标注 | 多层 | contracts | `feat/naming-unify` | ✅ 可开工 | ⚠️ 跨多文件,串行做 |

**并行建议**:一次并行起多个 Agent 时,**优先挑"✅ 独立"的包**(回测/pipeline/registry/采集器),它们改各自新文件夹零冲突;"⚠️" 的两个任务改现有共享文件,单独排、单写者做。

## 三、阻塞 / 待用户拍板

| 编号 | 事项 | 卡在哪 |
|---|---|---|
| N1 | 选股筛选具体规则 | 待用户给口径(现用默认预设,`screener/` 留口子) |
| N2 | 财报分析框架 | 待用户单独讨论(看什么/产出/哪些用 LLM,`financial_report/` 留口子) |

> 已解锁历史阻塞:Q7 大模型 API(deepseek-v4-pro 已接入)。

## 四、已完成(近期里程碑 · 详见[开发日志](日志/开发日志.md))

| 里程碑 | 说明 |
|---|---|
| P0–P4 数据/分析/展示主干 | 技术/基本面/公告/资金流/新闻情绪 + 预测 + Web 四页 |
| 组合层 portfolio | `analysis/portfolio.py`:方案2 组合层共性汇总(池级维度) |
| 报告层 report | `report/builder.py`:个股/汇总报告生成(221行,已实现) |
| P2-C 新闻情绪(LLM) | news 采集 + L1 抽取 + 三层归类 + 情感聚合 + 缓存;Web 情绪面板 |
| P4.0 接入大模型 | deepseek-v4-pro @ 内部网关(env 注入,脱敏) |
| 阶段 A 规范固化 | contracts 数据契约 + 架构设计/开发规范权威化 v1.0 + Git 铁律 |
| archfix §9.2/§9.3 | serialize 契约校验 + 展示层依赖修复(chart 视图) |
| **情绪打分接入**(模式B) | 政策/UGC 接 LLM 打分 → 情绪三层加权(新闻0.5/政策0.3/舆情0.2);000021 股吧实测净情绪-0.7偏空 |
| **情绪并入决策**(模式B) | 净情绪分并入 `predict.买卖倾向`(权重2,向后兼容);情绪偏多→偏买入实测生效 |
| 架构详解 | 信息流转+任务流程图 + 逐层职责 + 策略/回测/存取层规划 |
| **策略层 strategy**(模式B) | 选股/评分/信号 三类可注册策略(`@strategy`),复用 screener 预设;7 策略 + 15 测 |
| **数据存取层 store**(模式B) | 薄仓储层(raw/记录/视图 统一存取),13 测;渐进迁移待后续 |
| 回测层设计 | [计划/回测层.md](计划/回测层.md):通用回测框架计划 |
| **回测层 BT.1**(模式B) | `tools/backtest/`:信号回测 engine + metrics + **防未来函数测试**;均线金叉@000021 实测超额-28%(诚实拦截差策略);10 测 |
| **政策采集**(模式B) | `collectors/policy.py`:东财新闻按政策关键词检索(真实源)+ region/行业打标;7 测 |
| **UGC采集**(模式B) | `collectors/ugc.py`:curl_cffi 解锁东财股吧 SSR 内联 JSON(真实源)+ 热度指标;11 测 |

## 五、认领指引

想接手?→ 从「待做」选一项(优先 P0)→ 填到「进行中」(带分支名)→ 建分支开发 → 自测 → push → 写开发日志 → 合 main → 挪到「已完成」。全程守 [开发规范.md](开发规范.md)。
