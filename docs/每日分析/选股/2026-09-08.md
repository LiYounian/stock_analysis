# 每日选股 · 2026-09-08（周二）——闭环未在门控窗口内完成，今日选股跳过

> ⚠️ 测试环境研究模拟，非投资建议。

## 结论：跳过

**2026-09-08 盘后闭环（pull_refresh）未在选股任务的 90 分钟门控窗口内完成，今日全A策略选股跳过。**

## 门控执行记录（自证时点）

- 本任务 `task_guard`：EXPECTED 18:36:00 / ACTUAL 18:36:23 / DRIFT 0m23s（`DRIFT_FLAG=ok`，本任务自身无漂移）。
- 盘尾复盘互斥门控：`RESULT=ready, REASON=done`（经验沉淀 v2026-09-08 已就绪并已读）。
- 闭环就绪门控：**未通过**。
  - `data/sync_receipts/2026-09-08.json`：**不存在**（闭环上传收尾未写）。
  - `data/analysis/2026-09-08/`：**不存在**（当日全部策略视图未生成）。
  - 有界轮询：18:38 起每 2 分钟重查一次收据文件，连续 45 次（满 90 分钟）至 ~20:09 仍未就绪。

## 为什么没就绪（根因，供闭环侧排查）

- `ops/launchd/pull_refresh.sh`（PID 60484）**18:25 启动，至 20:09（≈1h45m）仍在运行**，远超实测 ~73min 的全A自采基线。
- 卡点在**基本面采集阶段（`collectors.fundamental`）**：日志显示逐只串行采集（…600248 → 600249 → 600250…），每只都 `baostock 登录成功/登出` 一轮，进度仍在 600xxx（沪市主板），后面 601/603/605/688/000/002/300 尚未开始。数据采集都未完成，策略视图（在采集之后才跑）自然更未产出。
- 因此本次并非"数据齐全但收据延迟"，而是**上游数据采集本身严重滞后**，用残缺/隔日数据抢跑会系统性劣化预测质量（违反闭环就绪硬门控与防未来函数红线），故不抢跑、如实跳过。

## 今日市场 β 背景（仅作记录，来自已就绪的盘中/广度数据，非选股依据）

- 09-08 为**高低切日**：创业板指领跌（收盘 −1.15%）、上证/中证500 相对抗跌，成长/科技退潮→价值/中盘承接（详见 `docs/每日分析/选股/日内_2026-09-08.md` 与经验沉淀 v2026-09-08 §1）。
- `data/breadth/2026-09-08.json` 已生成（15:05 收盘广度快照），但 `market_forecast.json` 与全A策略视图未产出，**不足以支撑逐票深度选股**。

## 遗留 / 建议

- 闭环侧：基本面采集逐只 baostock 登录/登出的串行成本过高，是本次超时主因，建议排查是否可批量/复用连接或与本任务 cron 解耦时序（**仅提议，不改代码、不改定时配置**）。
- 明日选股任务开工时若 `docs/每日分析/选股/2026-09-08.md` 已存在（本文件），按 SOP 不覆盖——本文件即当日跳过留痕。
